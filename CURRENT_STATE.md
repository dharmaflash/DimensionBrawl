# Current State

Last updated: 2026-06-22 KST

## Repository

- Git repository is initialized at `C:\Git\DimensionBrawl`.
- Main branch is `main`.
- Raw imported asset packs are local-only under `Assets/_Imported/`.
- Game-owned work should live under `Assets/_Game/`.
- Raw VFX packs are local-only under `Assets/_Imported/AssetStore/VFX/`; commit only curated effect prefabs/profiles promoted under `Assets/_Game/Art/VFX/`.
- Android is the mobile-first build baseline. The shared Android Build Profile lives at `Assets/Settings/Build Profiles/Android.asset`, uses the Android target, and should be cherry-picked by UI branches before mobile HUD or scene-flow work.

## Committed Baseline

- Unity project baseline exists.
- Combat reference docs and datasets are imported.
- `Assets/_Game/DesignDocs/CINEMACHINE_INGAME_CUTSCENE_REFERENCE_RESEARCH.md` and `Assets/_Game/DesignData/CinemachineIngameCutsceneReferenceDataset.json` are imported as the active Unity Cinemachine/Timeline in-game cutscene reference baseline for boss intros, summon/ultimate cut-ins, dialogue staging, camera shot sequencing, input/AI/time locks, impulse, Timeline signals, and gameplay camera return.
- `COMBAT_V1_SPEC.md` now defines the first fixed-rear boss-barrage lane + summon-first combat slice. The previous direct melee action foundation is preserved as a movement/camera/enemy authoring checkpoint, not the new product north star.
- `SummonLaneSpace` is the first runtime lane-space contract for the fixed-rear summon slice. It defines lateral width, backline, player forward boundary, boss-proxy distance, and summon-entry/frontline distance; player movement clamps to the player zone while summon/frontline battlefield coordinates remain valid beyond the player boundary and can cross player lane rails when a summon role/pattern needs it.
- `SummonEnergyLadder` is the first shared `EN LV1~LV3` combat-resource foundation. It samples player forward-risk position through `SummonLaneSpace`, applies serialized gain multipliers, uses serialized forward-risk band boundaries for `BackSafety`/`MidCharge`/`ForwardRisk` tuning, exposes the currently available tier for `Skill1`/`SummonSlot1`, accepts explicit reviewed encounter reward pulses, and resets to empty LV1 charging on spend.
- `SummonLaneAndEnergyTests` covers the first fixed-rear summon lane guarantees: player positions clamp before the forward boundary, summon entry/frontline positions can exist beyond that boundary, summon/frontline battlefield coordinates can cross player lane rails, forward-risk position charges EN faster than the backline, explicit EN reward pulses can open a current-tier spend, and spending EN resets the ladder.
- `BossBarragePatternProfile`, `BossBarrageEmitter`, and `BossBarrageProjectile` are the first boss-proxy projectile-pressure foundation. The default `NeedleLock`-style profile uses reference-backed minor telegraph/early-pressure timing, tighter projectile spread near the forward boundary, wider spread near the backline, a prewarmed projectile pool, hostile-team damage through `CombatHealth`, and explicit lifetime/hit cleanup. `BossBarrageEmitter` can now cycle a small authored pattern sequence and exposes whether the active wave is regular `Basic` pressure or a cost-spent priority `Costed` pattern for HUD/tests. The first review sequence is `NeedleLock -> CoverFire -> EscortScreen -> LayeredSalvo -> StaggeredCrossfire -> TwinSweep -> LeftClamp -> RightClamp -> PunishNet -> LinePressure`, where `CoverFire` suppresses the authored lane-center approach path without tracking the player's current side, `EscortScreen` adds alternating left/right curtain pressure around the escorted center path to foreshadow future summon/frontline answers, `LayeredSalvo` adds reference-backed three-row depth pressure based on multi-round barrage reads, `StaggeredCrossfire` adds slower crossed pairs from NIKKE-style heavy projectile pressure and Azur Lane-style multi-round barrage reads, `TwinSweep` uses a twin-column shape, `LeftClamp`/`RightClamp` use mirrored side-clamp pressure, `PunishNet` adds a player-centered net that tightens with forward-risk EN positioning, and `LinePressure` adds a committed rail threat that points toward a future Tank-screen summon answer without adding a full boss phase system.
- `BossBarragePatternProfile` now also carries data-only shared skill grammar metadata: a lane skill pattern family, a boss-only/player-skill/PvP-candidate transfer mode, and authoring notes for player-skill translation plus counterplay. This preserves the future option to reuse patterns as costed readable player skills without copying boss patterns into player basic fire or adding a broad shared skill system now.
- `BossBarrageLaneTelegraphPresenter` now reads pattern-specific telegraph color/scale data from `BossBarragePatternProfile`, so `LinePressure` previews as a narrow cyan depth rail while `LayeredSalvo` previews as wider magenta row plates before release. This keeps the first two boss skill patterns readable in-world without adding a new boss phase or VFX system.
- `BossPressureCostLadder` is the first boss-side `Cost LV1~LV3` combat-resource mirror for the fixed-rear lane review. It samples the boss proxy's forward-commit position through `SummonLaneSpace`, charges faster when the boss moves toward the contested frontline and slower when it remains safely back, and resets to empty LV1 charging on spend.
- `BossPressurePositionController` is the first boss-side lane-position pressure owner. It reads `BossPressureCostLadder` readiness and moves the explicitly bound boss proxy from a safe rest position toward a forward-commit risk position as boss cost builds, then eases it back toward rest when review logic disables boss pressure actions. It does not choose boss patterns, fire projectiles, spawn summons, or own encounter state.
- `BossPressureActionDirector` is the current narrow boss-side cost spender. It reads `BossPressureCostLadder`, chooses among explicitly authored action slots, and asks `BossBarrageEmitter` to queue one priority pattern. The current review slots are LV1 `LinePressure` as proactive skill pressure, LV1 `EscortProbeFrontlineCheck`/`EscortScreen` as proactive summon-pressure, LV2 `SummonSlot1PressureBlock`/`EscortScreen` as a player-summon response, and LV3 `PunishNet` as overextend punishment. Each slot now records its intended stage-loop role plus player/summon response plan, so boss cost spends stay tied to the fixed-rear PvP-like exchange instead of becoming isolated projectile spam. In the boss-barrage review scenes it also holds LV1 cost when the next-tier gated pressure action is currently open, so boss pressure can visibly reach LV2 summon-pressure instead of always dumping the first available tier. It is not a broad boss phase manager, wave spawner, summon spawner, or reward owner.
- `BossPressureActionDirector` slots can now be gated to the short player-summon response window and an observed player summon tier. The current LV2 `SummonSlot1PressureBlock` boss-pressure slot uses that gate, so it reads as a boss answer to the player creating a frontline summon instead of firing as a generic costed pattern. Only authored response-gated slots receive player-summon response priority or consume/count that response window; proactive boss pressure can still fire without claiming the summon answer. When the director is intentionally holding LV1 for an already-open next-tier summon response, it may keep that response window above a small serialized floor using a bounded extension budget, so the boss can actually reach the authored LV2 answer without turning the window into an endless wait.
- `BossSummonPressureAction` is the current boss-side summon-pressure owner. It releases one authored enemy-team pressure proxy with tiered battlefield placement, lifetime, scale, advance, and intercept-screen settings, then exposes active proxy/screen/intercept state for review HUD/tests. `DB_BossSummonPressure_SummonCaller` now carries tier readouts (`LV1 Escort Probe`, `LV2 Pressure Screen`, `LV3 Clamp Guard`) so boss-side cost spends read as escalating pressure roles, not only bigger proxy numbers. Its enemy-team pressure screen now also blocks hostile `LaneActionProjectile` shots from player/summon actions, tracks cumulative intercepts, and lets the review pocket read when a boss pressure screen blocked the player's summon-follow-up `Skill1`, so boss summon pressure can participate in the PvP-like projectile exchange instead of being only a visual read. It still does not own boss cost, boss pattern choice, wave spawning, rewards, or a full summon roster.
- `SummonLaneAndEnergyTests` now also covers boss-barrage spread shaping, projectile hostile-only damage, and pooled boss-side projectile emission. This remains boss-barrage foundation; it is not a full boss phase system, Skill1, or SummonSlot1 implementation.
- `Assets/_Game/Scenes/ActionFoundationBossBarrageLaneReview.unity` is the first authored fixed-rear boss-barrage lane review scene. It binds the existing player movement, `SummonLaneSpace`, `SummonEnergyLadder`, a far boss proxy, `BossBarrageEmitter`, a visible `NeedleLock` projectile prefab/pattern, fixed rear camera settings, explicit boss target candidate wiring, and player/summon boundary markers.
- `Assets/_Game/Scenes/ActionFoundationBossSummonDuelReview.unity` is the focused duel review scene for the current boss-barrage pivot. It keeps the same corridor map, disables the close-threat pocket sample, targets the far boss only, warm-starts player EN and boss cost, and uses `BossSummonDuelReviewOwner` plus the existing HUD to verify repeated boss cost pressure, boss summon-like pressure, player `SummonSlot1` use, summon projectile blocking, post-block `Skill1` response damage, and boss damage in one loop. The duel owner now shows authored clear/fail markers and stops EN gain, boss barrage, boss cost gain, and boss costed actions when the duel reaches clear/fail. It is a review scene, not a production stage, boss phase system, full summon roster, or wave spawner.
- `BossBarrageVisualCueDriver` now validates that each current boss barrage pattern cue maps to real promoted humanoid Animator triggers for windup/release presentation. It also listens to `BossPressureActionDirector.ActionQueued` so boss cost spends for `SkillPattern`, `SummonPressure`, and `PunishOverextend` create a short in-world trigger/color/pulse read instead of existing only as HUD text. This keeps future boss model swaps honest: a missing animation trigger should fail validation instead of silently dropping the boss read.
- Boss barrage pattern reads now also choose pattern-specific world VFX cues through `BossBarrageVisualCueDriver.PatternAnimationCue`: `NeedleLock`/`CoverFire` lean on shot cues, `EscortScreen`/`StaggeredCrossfire` lean on summon/fan pressure cues, `LayeredSalvo` uses heavy impact cues, `PunishNet` uses guard-break cues, and sweep/clamp/line patterns use line-pressure cues. The lane-review scene and tests require those cue ids to exist in the shared combat VFX profile so boss pressure no longer collapses into one generic aura/release effect.
- `PF_BossBarrageProjectile_NeedleLock`, `DB_BossBarrage_NeedleLock`, and the boss-barrage lane marker materials are game-owned `_Game` assets for the review slice. `BossBarrageEmitter` keeps both component and prefab-root serialized projectile references so Unity scene serialization persists the authored prefab reference without falling back to raw paths.
- `PlayerSkill1Action`, `PlayerSummonSlot1Action`, `SummonFrontlineProxy`, `SummonFrontlineClash`, `SummonFrontlineProxyPresenter`, `SummonPressureScreen`, `SummonPressureScreenPresenter`, and `LaneActionProjectile` are the first narrow Skill1/SummonSlot1 runtime slice. `Skill1` spends the current available EN tier and fires an immediate player-side lane projectile. `SummonSlot1` spends the current available EN tier, shows a magic-circle entry cue directly in front of the player body, activates a visible frontline summon proxy that can advance beyond the player boundary into the contested frontline, applies narrow hostile summon body-clash feedback through `CombatHealth`, opens a short ally pressure screen that carries the active summon tier, visibly pulses/flashes with tier-aware color, and briefly punches its proxy-local shield visual while intercepting limited hostile boss projectiles through trigger contact and a small bounded overlap scan, exposes the actual pressure-screen intercept tier/count for review-state rules, fires a short summon-owned counter bolt when the screen absorbs boss fire, and launches same-concept stronger LV1/LV2/LV3 summon-assist projectiles from that proxy using summon battlefield coordinates, not player movement clamps. `DB_SummonSlot1_ShieldBreaker` now also carries tier readouts (`LV1 Guard Entry`, `LV2 Frontline Push`, `LV3 Break Window`) so the review HUD and tests treat the tier ladder as same-summon escalation, not only bigger numbers.
- `PF_SummonSlot1Actor_Proxy` now presents the promoted `ShieldBreakerElite` visual as the primary player summon read while keeping the root proxy mesh hidden and limiting tier-color pulsing to the small `TierPulseCore`. `PF_BossSummonPressureActor_Proxy` follows the same presentation rule with promoted `AuraCaptainElite` as the primary boss pressure-summon read and `TierPressureCore` carrying the tier color pulse. The review mobile HUD now shows three top-right summon slots: functional `S1 SHIELD`, `S2 ARROW`, and `S3 TANK` entries with shared EN LV/ready state. Slot2/Slot3 are review-only support prototypes: `S2` uses a promoted `BacklineShooter` marksman actor and volley projectile profile, while `S3` uses a promoted `FinalStandCommanderElite` vanguard actor with a short screen/intercept profile. They are not a production roster, inventory, rarity, or upgrade economy.
- The current boss-barrage review deliberately scales all active summon actors to three times their previous actor-profile size: player `SummonSlot1` is `2.7/3.24/3.84`, player `SummonSlot2` is `2.85/3.15/3.48`, player `SummonSlot3` is `3.15/3.54/4.02`, and boss summon pressure is `2.76/3.36/4.08` for LV1/LV2/LV3. This is a readability/pass-presence tuning change only; summon damage, health, projectile, screen, and EN values stay unchanged.
- The active summon baseline is actor-first: cheap/frequent, ranged support, tank/vanguard, breaker, healer, and later roles may differ in cost, size, durability, lifetime, projectiles, screens, fields, or cadence, but normal summon slots should still enter from the player side as body-bearing actors, fight hostile summon actors that block the path, and pressure the opponent when unblocked before any production roster/economy work begins.
- `PF_PlayerSkill1Projectile_LaneBolt`, `PF_SummonSlot1Projectile_AssistBolt`, `PF_SummonSlot1EntryCue_MagicCircle`, `PF_SummonSlot1Actor_Proxy`, and their game-owned action-foundation materials are authored `_Game` prefabs/materials for the current review slice. They are placeholders for readability and ownership, not final summon art.
- `DB_SummonPresentation_PlayerShieldBreaker` and `DB_SummonPresentation_BossAuraCaptain` are data-only presentation candidate profiles for the first player summon and boss-side pressure proxy. They bind the current actor prefab, promoted visual source, Animator Controller, shared VFX cue profile, and replacement notes without owning summon EN, boss cost, projectile timing, tier values, or encounter results.
- `DB_PlayerAction_BossBarrageLocalDefense` is the boss-barrage review scene's one-hit `BasicDefenseAttack` profile. It reuses the existing player action owner for close-threat defense only, without reviving the melee-combo-first direction or giving the player a boss-DPS route.
- `ActionFoundationBossBarrageLaneReview.unity` now includes one authored close-threat soldier, a review-only `BossBarragePocketReviewOwner`, and a temporary `BossBarrageLaneReviewHud` so the fixed-rear lane slice can show HP, close-threat HP, boss HP, EN fill/readiness, forward-risk gain, boss cost/position pressure, current boss-barrage pattern/windup/projectile count, active boss summon-response window/held next-tier response, last boss pressure response plan, Skill1/SummonSlot1 readiness, active summon proxy/projectile/shield block state, summon follow-up timing, and one clear/fail condition. The pocket owner now starts a readable `1.35s` boss-pressure relief beat when the close threat is defeated, requires a `SummonSlot1` pressure block after that defeat, opens a tier-aware summon follow-up window, grants a tier-aware EN follow-up pulse for immediate `Skill1` response, carries overflow EN into the next tier instead of discarding it, observes whether that follow-up Skill1 actually damages the boss proxy, and requires that confirmed hit before clear. The follow-up tuning uses a roughly `3.25s` LV1 boss-pressure break, a `2.25s` LV1 response window, and a `0.75s` hit-confirm settle beat so a successful Skill1 hit reads as an immediate result instead of waiting for the whole pressure break to expire. Its LV3 pressure block still gives a longer `4.0s` break and `3.1s` follow-up window so high-tier summon timing feels meaningfully safer without making LV1 too slow. Its objective cue now names the current `SummonSlot1` tier readout, such as `LV1 Guard Entry`, so the reviewed loop reads as same-summon escalation instead of a generic button prompt. If the follow-up misses or a boss pressure screen blocks the follow-up `Skill1`, the existing missed-response camera/VFX cue fires once, the current follow-up window closes, boss pressure resumes, and a later `SummonSlot1` block can reopen the Skill1 window. The owner then stops EN gain, boss barrage, boss cost gain, and boss costed actions when the review pocket reaches clear/fail so the result state does not keep charging in the background.
- `BossBarrageLaneReviewHud` now defaults to a compact play-read HUD that shows objective, phase, current combat cue, frontline state, and only active hints while leaving the detailed telemetry readouts available behind `showDetailedTelemetry`. This keeps the one-pocket review screen from covering the lane/VFX read during normal play checks, while tests can still assert the detailed pressure-response strings.
- `ActionScreenCuePresenter` now keeps active high-priority screen reads from being overwritten by lower-priority cue spam: follow-up window/hit/miss reads outrank boss pressure, and boss pressure outranks routine player cue flashes. It also listens to player damage plus `SummonEnergyLadder` risk/ready/spend state, so taking damage, entering the forward-risk EN zone, opening an EN tier, and spending EN produce small low-priority screen cues alongside the existing world VFX. `PlayerCombatVfxCueDriver` now also plays promoted `PlayerDamaged` and `PlayerCritical` world cues through the shared combat VFX profile when the player is hit or crosses the critical health threshold. This keeps the one-pocket damage/readiness timing and forward-risk charging signal visible during dense projectile/VFX moments without adding more HUD text.
- `BossBarragePocketReviewOwner` now emits explicit pocket clear/fail events, and `ActionScreenCuePresenter` turns those into short result screen cues. This gives the one-pocket review a readable end beat in the play view instead of relying only on HUD state text or marker objects after combat stops.
- `BossBarragePocketVfxCueBridge` routes clear/fail result events into world-space VFX as well as screen cues. `PocketFailed` now uses the promoted MagicMissiles light-impact asset while the existing fail accent supplies the red close-punish sweep, so failed runs leave a visible defeat read without washing out the player, boss, or summon silhouettes, and `ActionFoundationBossBarrageVisualSmokeTests` captures normal combat, pocket clear, and pocket fail frames under `Logs/` to guard against blank, washed-out, or unreadable result presentation.
- `DB_BossBasicFire_LanePoke` now uses a `1.95s` weak two-shot cadence with `3.6` damage per shot so regular boss fire leaves slightly cleaner visual gaps between committed patterns while keeping baseline damage pressure near the previous pocket budget. The profile intentionally leaves `ProjectileMaterial` empty so the authored projectile prefab keeps its promoted VFX material stack instead of being overwritten by a plain material.
- The current one-pocket balance guard uses `\\DESKTOP-69817L3\ArkData\PGR_Tutorial_Stage_Data_2026-06-19` as a shape reference, not runtime data: PGR separates guide progression, UI guide steps, fight-node context, and tutorial text signals, and the reviewed fight-node cue rows show short `1.2s` trigger beats, `3.0s` combat-read beats, and `5.0s` major countdown/warning beats. Aether Gazer ArkData stage topology/wave rollups were checked only as broad role/topology context. `ActionFoundationBossBarrageLaneReviewSceneTests` now protects the review pocket so the `1.35s` block-opportunity cue, `3.25s` LV1 pressure break, `2.25s` follow-up window, and `6.4s` relief spacing keep a readable short-cue -> action-window -> recovery shape without importing external data or raw assets.
- The current one-pocket Skill1 payoff is tuned for a visible boss-health response after the summon block: LV1/LV2/LV3 Skill1 damage is `84/104/128` per projectile, so LV1 clears about one fifth of the review boss HP while higher tiers remain costed tier payoffs instead of basic-fire replacements. This keeps the ArkData-derived timing shape intact while making the successful block -> Skill1 follow-up read as a real one-round reward instead of a barely visible confirmation hit.
- The boss-barrage review HUD now treats ranged fire as a mobile-first cross-platform verb: `Fire` is triggered through the HUD fire button, `F`, gamepad trigger/button, or bound Input Actions; non-button screen drag feeds `Look` / `TargetBias`, while the Fire button remains a pure trigger with no drag-aim joystick path. While aiming, that non-button target-bias input can peek the fixed-rear camera up to 45 degrees left/right, and the review HUD exposes an explicit temporary `Q`/`E` keyboard peek hook for PC testing. Releasing the target-bias input now holds the current aim-camera yaw while ranged aim/fire remains active, then naturally recenters when aim/fire ends. The review reticle stays fixed at screen center like an FPS reticle, basic ranged fire uses the center camera ray after aim-camera peek instead of offsetting the reticle viewport point, aim peek moves the shoulder camera position from a player-based rig origin so the player stays near the same screen anchor, and ranged aim asks player movement to keep the body facing the aim/camera forward direction so left/right movement reads as strafe movement. Review-scene mouse fallbacks no longer require simultaneous left-click fire plus right-click aim, raw left/right mouse fallbacks for ranged fire/aim are disabled by serialized fields unless a reviewed scene explicitly opts in, and `RangedAim`/`Skill1` no longer carry hidden Q/E keyboard fallback test keys.
- The boss-barrage lane review scene now routes successful `Skill1` and `SummonSlot1` spends into short bounded `ActionCameraCueDriver` cues through explicit player action references. It also uses a review-only `BossBarragePocketCameraCueBridge` to route the summon follow-up window, confirmed follow-up boss hit, and missed follow-up response into presentation camera cues. This keeps skill/summon feedback in the presentation camera layer instead of growing the review HUD or moving camera ownership into player/summon gameplay code.
- The boss-barrage lane review scene now routes `BossBarrageEmitter` windup/wave-fire events and `BossPressureActionDirector.ActionQueued` cost-spend events into a dedicated presentation-only `BossBarrageCameraCueDriver`. This adds short bounded boss-pressure camera reads without changing HUD/UI ownership, boss pattern selection, projectile damage, summon behavior, or boss phase logic.
- `ActionFoundationBossBarrageLaneReviewSceneTests` covers review-scene wiring, player-bounded versus summon-crossing battlefield behavior, the authored `NeedleLock -> CoverFire -> EscortScreen -> LayeredSalvo -> StaggeredCrossfire -> TwinSweep -> LeftClamp -> RightClamp -> PunishNet -> LinePressure` boss-pattern sequence, visible pooled boss-side projectile emission, immediate tiered Skill1 spend/fire with boss hit-receiver damage, SummonSlot1 LV3 proxy activation/projectile travel beyond player lane rails, SummonSlot1 pressure-screen prefab/presenter wiring, active LV3 visible intercept budget and HUD-readable pressure-screen state, bounded overlap interception for already-overlapping hostile boss projectiles, summon counter-bolt damage against the authored boss hit receiver, local-defense damage against the close threat, readable boss-pressure relief after close-threat defeat, one pocket clear condition that requires `SummonSlot1` to block boss fire after the close threat is defeated, tier-aware summon follow-up windows and EN pulses, LV3 summon-block overflow carrying forward into a LV2 follow-up choice, Skill1 response and actual boss-hit confirmation during that follow-up, the short post-hit clear settle beat, follow-up window/hit/miss camera and in-world VFX cue routing, no-clear/missed-follow-up retry behavior, boss-pressure break timers after that correct block, and clear/fail end-state stopping for EN gain, boss barrage, boss cost gain, and boss costed actions.
- `ActionFoundationBossSummonDuelReviewSceneTests` now covers the focused duel scene's full exchange wiring: boss skill-pressure and summon-pressure requirements, boss LV1-to-next-tier hold policy, boss pressure summon profile/readiness, `SummonSlot1` plus `SummonSlot2`/`SummonSlot3` support bindings, support summon tier readouts, HUD/mobile HUD references, and the review owner's clear-condition counters for boss screen block, ally summon block, Skill1 response damage, and total boss damage. It also verifies that S2/S3 support summons can spend EN in the duel scene and create visible support actors/projectiles rather than existing only as HUD buttons.
- `ACTION_FEEL_TARGETS.md` defines the movement, fixed rear camera, forward-risk `EN LV1~LV3` energy, local-defense attack, tiered skill/summon call, dodge, projectile/hit, and enemy/boss-proxy feel quality targets that action work must improve.
- `ACTION_FOUNDATION_OWNERSHIP.md` defines the narrow runtime ownership split for the first action-feel implementation.
- `ACTION_FOUNDATION_TESTING.md` records the first test setup, control map, reference-backed values, first-pass deviations, and deliberate exclusions.
- `Assets/_Game/Scenes/ActionFoundationTest.unity` exists as the first authored action-feel inspection scene.
- `ActionFoundationTest.unity` uses game-owned URP test materials under `Assets/_Game/Art/Materials/ActionFoundation/` so the inspection scene does not rely on Unity's built-in default material or render pink/missing-shader placeholders.
- The player inspection placeholder has `PlayerDodgeFeedback`, a presentation-only dodge tint driven by `PlayerActionController` dodge events, so the dodge window is visible even before real player animations are wired.
- `ActionFoundationTest.unity` now keeps `Player_CombatGirl_ActionFoundation` as the gameplay root and mounts a curated `CombatGirlSwordShield_PlayerVisual` child under that root; movement, combat, health, and feedback ownership remain on the existing root components.
- The CombatGirl visual slice promotes only selected game-owned assets into `_Game`: one model/avatar, sixteen curated clips including `SS_StartRun.fbx`, `SS_StopStep.fbx`, `SS_TurnLeft90.fbx`, `SS_TurnRight90.fbx`, `SS_DodgeForward.fbx`, `SS_DodgeBack.fbx`, `SS_DodgeLeft.fbx`, `SS_DodgeRight.fbx`, and `SS_Attack1` through `SS_Attack5`, one Animator Controller, primary albedo textures, and Unity Toon Shader materials. It should not reference `_Imported/`.
- `CombatGirlWeaponSocketBinder` is attached to `CombatGirlSwordShield_PlayerVisual` and pins `add_weapon_l` / `add_weapon_r` to `hand_l` / `hand_r` with captured offsets, so the One Hand Base StopStep candidate cannot leave the sword or shield floating.
- The old capsule/sword proxy objects remain inactive fallback/reference objects in the scene. The active player hit/dodge feedback now targets CombatGirl renderers, and the old cyan idle tint is disabled so authored material colors can show between hit/dodge flashes.
- `ActionFoundationTest.unity` now has an `ActionCameraCueDriver` on the action camera. It listens to player run start, stop-settle, sharp turn, dodge, basic attack start, and successful hit events, then requests short bounded additive offset/FOV/distance/focus cues from `ActionCameraController`.
- `ActionCameraController` now uses a lower landscape ARPG third-person orbit preset instead of deriving the camera position from instant player rotation. Right mouse drag, gamepad right stick, or the mobile orbit hook can rotate the camera; player-facing assist recenters slowly instead of hard-snapping.
- `PlayerMovementController` keeps the current input response but now uses a promoted `StartRun` trigger, `0.26s` stop-settle, `0.16s` final-input hold, `0.06s` Animator move damping, a `0.24` stop-settle MoveSpeed floor, a `StopStep` trigger, and promoted 90-degree turn triggers for large running direction changes.
- `SS_StopStep` uses the reviewed One Hand Base run-to-idle candidate trimmed to source frame `4`, Animator speed `1.45`, and `0.015s` no-exit-time StopStep transitions so key release reads sooner.
- `PlayerActionController` now supports a five-hit basic chain using promoted `Attack1` through `Attack5` animation requests. The chain remains a single basic attack route with explicit startup, active, recovery, buffer, dodge-cancel, damage, hit-radius, hit-distance, and reserved hit-stop hint values for each hit, with a `0.10s` queue-open gate and `45%` recovery-chain point so later hits do not feel like buffered input drops after hit 3.
- `PlayerActionController` dodge now routes through directional CombatGirl Quickshift triggers (`DodgeForward`, `DodgeBack`, `DodgeLeft`, `DodgeRight`) and `PlayerMovementController` decays the dodge burst over the `0.56s` movement window so it reads more like a lunge/roll than a constant slide. Held movement input dodges along the current move direction; no-input dodge moves backward from the current facing direction.
- `PlayerActionController` attack overlap ignores colliders under the player root and resolves `CombatHealth` through parent objects, so authored CombatGirl body/weapon colliders do not block soldier hit validation.
- `CombatHealth` now includes `AllySummon` as a future player-side team and routes friendly-fire/hostile checks through `CombatTeamUtility`, so the first enemy and later summon actors can share one small team contract.
- `CombatHealth` now exposes a narrow `DamageModifying` hook with `DamageModificationContext`; elite shields, armor, aura protection, and future summon protection can modify incoming damage without making `CombatHealth` know concrete enemy pattern ids.
- `CombatHitFeedback` no longer applies normal-hit global slow motion or hit-stop. It handles damage flash and death color only; time-scale effects are reserved for a later explicit perfect-dodge, counter, ultimate, or authored cue bundle slice.
- `CombatVfxCueProfile`, `CombatVfxCuePlayer`, `PlayerCombatVfxCueDriver`, and `EnemyCombatVfxCueDriver` are the first presentation-only VFX hook. They listen to player action, player damage/critical state, enemy pattern, damage, and death events, then play authored cue-profile prefabs through a small per-prefab pool instead of hardcoding asset paths or spawning unbounded effects in gameplay code.
- `SummonFrontlineProxyPresenter` now requests promoted combat VFX cues for summon actor entry, attack, clash, damage, and death states. `PlayerSummonSlot1Action`, `PlayerSupportSummonSlotAction`, and `BossSummonPressureAction` inject the existing combat VFX cue player into spawned summon actors, so player summons and boss pressure summons do not rely only on material tint/pulse feedback in the boss-barrage review loop.
- `CombatVfxCuePlayerAudioTests` now also locks promoted MagicMissiles audio on combat state cues (`EnemyHit`, `EnemyDeath`, `PlayerDamaged`, `PlayerCritical`, `EliteShieldSignal`, `EliteArmorBreakSignal`, `EliteSummonSignal`, `SummonFollowupWindow`, `SummonBlockOpportunity`, `PocketCleared`, `SummonFollowupMissed`, `PocketFailed`) in addition to the ranged shot and projectile audio checks, and it verifies every nested cue AudioSource is cue-player driven rather than `PlayOnAwake`, so hit/death/player-state/shield/armor-break/summon/follow-up/result/projectile-impact reads do not silently regress to visual-only or stray autoplay prefab cues.
- `DB_CombatVfxCues_ActionFoundation` now maps player attack/dodge, enemy windup/active/hit/death, eight authored soldier attack profiles, five elite signal profiles, and the boss-barrage summon follow-up window/hit/miss reads to promoted `_Game/Art/VFX/CombatCues` prefabs. `EnemyCombatVfxCueDriver` resolves pattern-specific cues through serialized profile references, not pattern-id branches.
- `SummonBlockOpportunity` and `SummonFollowupWindow` now use distinct promoted cue prefabs instead of sharing `DB_VFX_EliteSummonSignal`: block opportunity layers the MagicMissiles pressure storm read, follow-up window layers the MagicMissiles arcane circle read, and both carry cue-player-driven `holy_shoot` one-shot audio. The combat VFX generator also preserves `PocketCleared`/`PocketFailed` cue mappings so future reapply passes do not erase result-state VFX while tuning the one-pocket loop.
- `BossBarragePocketVfxCueBridge` now also reacts to pocket clear/fail result events with explicit `PocketCleared`/`PocketFailed` combat VFX cue ids, reusing promoted MagicMissiles summon-state and light-impact reads so the one-pocket review does not finish with only HUD text, marker toggles, or screen flash.
- The first combat cue prefabs are safe game-owned mesh cue prefabs generated under `_Game/Art/VFX/CombatCues`; `CombatVfxCueVisual` drives their short scale/color/spin/lift/forward-travel fade so ActionFoundation cues do not read as broken particle shards. The ranged soldier `LinePressure` and `RetreatShot` active cues use elevated `ProjectileCore`/`ProjectileTrail` meshes plus authored forward travel as shot readability only; damage still belongs to `CombatAiPatternProfile` hit shapes and `BasicSoldierEnemy` execution. They do not directly embed raw vendor-prefab dependencies from `_Imported`. If a vendor effect becomes the final visual choice, promote that specific source prefab plus required materials/textures/meshes in a reviewed slice before wiring it into the cue profile.
- `ActionFoundationTest.unity` now gives `Enemy_SciFiSoldier_ActionFoundation` a `CombatTargetSensor` with one authored player-health candidate, serialized search radius `12m`, and retarget interval `0.2s`; the sensor evaluates supplied candidates instead of scene-wide searches and does not own summon spawning, slots, cooldowns, or UI.
- `BasicSoldierEnemy` now declares serialized `enemyTypeId = SciFiSoldier.Basic` and `patternId = ClosePunish`, while model, Animator Controller, trigger names, and future pattern variants remain prefab-level data instead of hardcoded asset paths.
- `BasicSoldierEnemy` now reads attack shape, windup direction lock, camera cue, telegraph sizing/color, and optional pre-attack reposition data from `CombatAiPatternProfile`; `RetreatShot` and `RetreatBlink` use the shared `Repositioning` state instead of pattern-id-specific runtime branches.
- `CombatAiPatternDeck` is the current general/elite pattern selector. `DB_BasicSoldier_GeneralPatternDeck` combines `ClosePunish`, `LungeStrike`, `RetreatShot`, `FanPressure`, `LinePressure`, and `RetreatBlink`; `DB_EliteSoldier_PatternDeck` combines `ClosePunish`, `GuardBreak`, `HeavyWindup`, `FanPressure`, `LinePressure`, and `RetreatShot`; `DB_EliteSoldier_PhaseTwoPatternDeck` is the phase-swap target.
- `CombatEnemyRoleProfile` is the current data-only role catalog for linear monster composition. Role profiles under `Assets/_Game/DesignData/Profiles/ActionFoundation/EnemyRoles/` combine existing pattern decks and elite trait profiles into 7 general soldier roles and 5 elite soldier roles for Entry Read, Break Gate, Backline, Pressure Rescue, Boss Break Handoff, and Final Stand.
- The current general soldier roles are `EntryProbe`, `CloseGuard`, `LungeChaser`, `LineCaster`, `FanSuppressor`, `BacklineShooter`, and `Skirmisher`. The current elite roles are `ShieldBreaker`, `AuraCaptain`, `SummonCaller`, `PhaseDuelist`, and `FinalStandCommander`. These are authoring data for future prefab/model swaps and summon-AI reuse, not a runtime wave spawner.
- `CombatEnemyArchetypeProfile` is the current data-only role-to-presentation candidate catalog. Archetype profiles under `Assets/_Game/DesignData/Profiles/ActionFoundation/EnemyArchetypes/` group compatible role profiles, point only to already promoted `_Game` object references, and record raw-pack promotion notes for assets that are not ready to assign yet.
- The current archetype catalog contains 3 mobile soldier candidates (`SciFiSoldier.Melee`, `SciFiSoldier.Ranged`, `SciFiSoldier.Elite`), 2 static FORGE3D turret candidates (`Forge3D.LineTurret`, `Forge3D.MissileTurret`), 3 promoted humanoid boss candidates (`HumanoidBoss.SummonCallerElite`, `HumanoidBoss.AuraCaptainElite`, `HumanoidBoss.FinalStandCommanderElite`), and 1 future dragon boss candidate (`DragonBoss.Future`). These are planning/authoring data for reviewed prefab promotion, not a runtime prefab builder, wave spawner, summon implementation, or boss system.
- First boss/proxy presentation should currently prefer humanoid caster/commander candidates such as `SummonCallerElite`, `FinalStandCommanderElite`, or `AuraCaptainElite`. Dragons are still valid later as large chapter bosses, set-piece bosses, or high-cost summons, but should not be forced into the first boss-barrage review before humanoid windup/release animation readability is validated.
- `Forge3D.LineTurret` now has a promoted `_Game` presentation-only visual candidate built from selected FORGE3D turret model/texture sources. Its archetype visual slot points at `PF_Enemy_FORGE3D_LineTurret_Presentation`, but its gameplay prefab remains null until a dedicated static-turret controller, hit shape, and reviewed beam cue are authored. `Forge3D.MissileTurret` remains promotion-pending until fixed-turret gameplay is accepted.
- `CombatEnemyRoleCandidateProfile` is the current data-only role-to-prefab review contract. Candidate profiles under `Assets/_Game/DesignData/Profiles/ActionFoundation/EnemyRoleCandidates/` bind each general/elite role to a primary archetype, authored role prefab, promoted visual source, optional static turret visual where applicable, and the shared combat VFX cue profile.
- `Assets/_Game/Prefabs/Enemies/ActionFoundation/RoleCandidates/` now contains 12 authored role review prefabs for `EntryProbe`, `CloseGuard`, `LungeChaser`, `LineCaster`, `FanSuppressor`, `BacklineShooter`, `Skirmisher`, `ShieldBreaker`, `AuraCaptain`, `SummonCaller`, `PhaseDuelist`, and `FinalStandCommander`. These variants keep role-specific starting pattern, pattern deck, elite trait subset, local VFX cue bindings, and scene-free target/camera ownership. They are review candidates, not a runtime wave spawner or encounter composition system.
- `LinearStageTemplateProfile` and `LinearStageSegmentProfile` are the current data-only stage design foundation. Stage templates under `Assets/_Game/DesignData/Profiles/ActionFoundation/StageDesign/` describe first-pass linear routes such as `S1-1 Break Gate`, `S1-2 Backline Signal`, `S1-3 Tank Rescue`, `S1-4 Heal Pocket`, and `S1-5 Boss Stand` using segment/pocket/objective/role-slot data only.
- Current linear stage templates record intended pressure rhythm, relief beats, future summon needs, and existing enemy role composition. They do not spawn waves, reference raw `_Imported` assets, assign gameplay prefabs directly, grant rewards, implement summons, or create boss phases.
- `PF_Enemy_SciFiSoldier_Melee_ClosePunish` is the first authored enemy prefab candidate under `Assets/_Game/Prefabs/Enemies/ActionFoundation/`. It is promoted from the reviewed close-punish soldier sample, carries local health/AI/sensor/Animator/telegraph/VFX/cue-driver references, keeps target candidates empty, and does not serialize the ActionFoundationTest camera controller.
- The `SciFiSoldier.Melee` archetype points its gameplay prefab at `PF_Enemy_SciFiSoldier_Melee_ClosePunish`, `SciFiSoldier.Ranged` points at `PF_Enemy_SciFiSoldier_GeneralDeck` with a promoted `SciFiSoldier_01` rifle visual, and `SciFiSoldier.Elite` points at `PF_Enemy_SciFiSoldier_EliteDeck` with a promoted `SciFiHeavyBattleArmor` visual. `Forge3D.LineTurret` has only a presentation visual candidate; `Forge3D.MissileTurret` and `DragonBoss.Future` remain planning/promotion-pending until reviewed slices exist.
- `Assets/_Game/Scenes/ActionFoundationEnemyPrefabReview.unity` is the manual Play-mode review scene for the first melee soldier prefab candidate. It keeps the current player/camera/test arena setup, removes the twelve-pattern enemy lineup, places one prefab instance named `EnemyPrefabReview_SciFiSoldier_Melee`, injects target candidates in both directions, and gives that instance the scene camera controller.
- `PF_Enemy_SciFiSoldier_GeneralDeck` is the second authored enemy prefab candidate under `Assets/_Game/Prefabs/Enemies/ActionFoundation/`. It is promoted from the reviewed `Enemy_SciFiSoldier_GeneralDeck` sample, starts from the close-punish profile row, keeps `DB_BasicSoldier_GeneralPatternDeck` assigned, uses a distinct promoted `SciFiSoldier_01 Commando` rifle visual plus assault-rifle Animator Controller and promoted `SM_SciFiAssaultRifle_01` weapon mesh, carries local health/AI/sensor/Animator/telegraph/VFX/cue-driver references, keeps target candidates empty, and does not serialize the ActionFoundationTest camera controller.
- `Assets/_Game/Scenes/ActionFoundationEnemyGeneralDeckReview.unity` is the manual Play-mode review scene for the GeneralDeck soldier prefab candidate. It keeps the one-enemy prefab review shape, places one instance named `EnemyPrefabReview_SciFiSoldier_GeneralDeck` at a farther initial distance, injects target candidates in both directions, and gives that instance the scene camera controller so deck row selection and camera cues can be checked without the twelve-pattern lineup.
- `Assets/_Game/Scenes/ActionFoundationEnemyRoleCandidateReview.unity` is the manual Play-mode review scene for all 12 role candidate prefabs. It keeps the current player/camera/test arena setup, places `EnemyRoleReview_*` prefab instances in a spaced review course, injects player/enemy target candidates, gives each enemy the scene camera controller, and avoids runtime wave spawning or production encounter composition.
- `Assets/_Game/Scenes/ActionFoundationStageBreakGateReview.unity` is the first route-composition review scene for `S1-1 Break Gate`. It keeps the current player/camera/test arena setup, places authored role candidate prefab instances in `EntryRead -> BasicPressure -> BreakGate -> Relief -> FinalStand` order, injects player/enemy target candidates, points the temporary test encounter at the final route enemy, and avoids runtime wave spawning, reward payout, summon behavior, or boss logic.
- `StageEncounterReviewOwner` is the review-only pocket tracker for `ActionFoundationStageBreakGateReview.unity`. It binds authored pocket anchors and placed enemy health references back to the `S1-1 Break Gate` template, exposes current pocket/objective cue/remaining enemies/route completion for tests and manual review, and must not become a wave spawner, prefab selector, reward system, summon system, boss phase owner, or production stage manager.
- `ActionFoundationStageBreakGateReview.unity` now uses selected Spring Isles environment dressing promoted under `_Game/Art/Environment/SpringIsles` for first-stage presentation review. The promotion path keeps raw ToonScapes packs local under `_Imported`, deletes raw `.tga` source texture copies from the promoted folder, and uses game-ready PNG texture copies under `_Game/Art/Environment/SpringIsles/Textures` instead of Git LFS source textures.
- `Assets/_Game/DesignDocs/ART_ASSET_STORAGE_WORKFLOW.md` and `Assets/_Game/DesignDocs/SPRING_ISLES_DEMO_ADAPTATION_PLAN.md` define the current art-source/depot policy and the next Spring Isles demo-quality adaptation path. Git remains for code, docs, authored scenes, data, and reviewed game-ready assets; raw packs and vendor demo scenes remain local-only until a deliberate Unity Version Control, Perforce, paid LFS, or equivalent art-depot decision is set up.
- `EnemyElitePatternController` is the current elite trait layer for `ShieldCycle`, `ArmorBreak`, `AuraBuffer`, `SummonPackage`, and `PhaseSwap`. It consumes `CombatAiElitePatternProfile` assets, modifies damage through `CombatHealth.DamageModifying`, protects explicitly authored aura receivers, activates pre-authored summon-signal objects, and swaps the attached soldier profile/deck for phase changes without runtime spawning or scene-wide ally searches.
- `ActionFoundationTest.unity` now includes twelve authored enemy samples: the original six action-foundation soldiers plus `Enemy_SciFiSoldier_RetreatShot`, `Enemy_SciFiSoldier_RetreatBlink`, `Enemy_SciFiSoldier_GuardBreak`, `Enemy_SciFiSoldier_GeneralDeck`, `Enemy_SciFiSoldier_EliteDeck`, and `Enemy_SciFiSoldier_EliteTraits`. The player target selector receives those twelve `CombatHealth` candidates explicitly.
- `EnemyAttackTelegraphPresenter` is attached to the basic soldier as presentation-only readability: it grows the attack marker through windup, flashes larger on active release, shifts warning color, uses a game-owned warning/highlight material with line markers, and temporarily offsets the promoted soldier visual so attacks stay readable.
- `Enemy_SciFiSoldier_ActionFoundation` now uses a promoted game-owned MaintenanceWorker visual under `_Game`, with a minimal Animator Controller and selected `Idle`, `Run`, `Attack`, `AttackCombo2`, `AttackCombo3`, `CrouchForward`, `CrouchIdleCombat`, `RepairHigh`, `RepairLow`, `TypeOnConsole`, `Turn90Right`, `Hit`, `HitHeavy`, and `Death` clips. The old capsule placeholder remains inactive as fallback/reference only.
- The basic soldier still owns timing and state through serialized `BasicSoldierEnemy` values; the promoted enemy Animator is presentation-only and receives `MoveSpeed`, pattern-specific prepare/attack triggers, light/heavy hit triggers, elite signal triggers, and `Death` requests without hardcoded runtime asset paths.
- The promoted MaintenanceWorker `Death` clip uses feet-based Y import so the death pose settles near the controller base instead of floating at the source clip root height.
- `PlayerMovementController` no longer consumes gamepad right-stick fallback for character facing; right-stick fallback now belongs to camera orbit unless an explicit look action/mobile look hook is wired later.
- `ProjectSettings/EditorSettings.asset` intentionally has Enter Play Mode options enabled with option value `3` to reduce repeated Editor play-mode reload friction during action-feel testing.
- Android PlayerSettings are the current cross-PC baseline: company `dharmaflash`, package `com.dharmaflash.dimensionbrawl`, landscape-left/right only, URP scripting define, IL2CPP scripting backend, and ARM64 architecture. PC/Standalone remains useful as a development target, but mobile layout, safe area, and touch actions are the default product assumptions.
- UI V1 branch work now uses Android/mobile-first landscape assumptions: UI test scenes are authored under `Assets/_Game/Scenes/UI/`, the shared scene shell uses a `2400x1080` landscape Canvas Scaler reference, content is parented under an authored Safe Area root, and EventSystems use `InputSystemUIInputModule`.
- UI presentation backgrounds for login and lobby now use a shared full-bleed scene shell root, while screen controls remain under the authored Safe Area root. This keeps wide mobile backgrounds covered without moving interactive UI into unsafe display regions.
- UI scene routing uses short fade-only transitions for immediate UI routes such as login to lobby and lobby to stage select; loading cards and async progress are reserved for routes with a visible wait reason, currently the stage-select to combat-HUD handoff.
- `UI_LobbyTest` now uses the first authored lobby PSD/PNG layer export under `Assets/_Game/UI/Lobby/Source/` and `Assets/_Game/UI/Lobby/Art/`. `PF_UI_LobbyScreen` contains an inspectable `LobbyArtRoot` with a full-bleed `LobbyBackgroundRoot`, a 16:9 `LobbyDesignFrame` UI layer stack, and a transparent `StoryPveButton` hit area over the main PvE card; currency, shop, storage, mail, event, and progression visuals remain static UI placeholders only.
- `PF_UI_LobbyScreen` also has a catalog-driven `LobbyTextRoot` above the authored lobby art for mock profile, notice, resource, PvE/PvP card, and bottom navigation labels. These labels are display-only placeholders and do not connect account, economy, PvP, mail, mission, event, shop, storage, or progression systems.
- Lobby text placement is now aligned from the PSD-exported guide PNGs under `Assets/_Game/UI/Lobby/Source/TextAreas/`; the guide bounds are converted to normalized RectTransform anchors so the mock text stays inspectable and mobile-landscape responsive without runtime UI generation.
- `PF_UI_LobbyScreen` now uses a small built-in entrance sequence on enable: `LobbyBackgroundRoot`, `LobbyDesignFrame`, and `LobbyTextRoot` reveal in order through `LobbyEntranceSequencePresenter` plus catalog motion data. V1 lobby enter polish currently uses Unity UI + coroutines only; no external tween package is required yet.
- UI V1 display data includes the canonical combat HUD action vocabulary from `COMBAT_V1_SPEC.md`, including `Move`, `Look`, `TargetBias`, `BasicDefenseAttack`, `Dodge`, `Skill1`, `Ultimate`, `SummonSlot1`, `SummonSlot2`, `SummonSlot3`, and `Pause`, with summon slots still visual placeholders only.
- Future production combat HUD direction: place boss-side HP and boss energy/cost at the top center, and place player HP/resource state at the bottom center. This should replace the current review/debug HUD information layout when the formal combat UI is authored.
- `ProjectSettings/EditorBuildSettings.asset` intentionally lists only the UI V1 test route scenes for the current contest/test build handoff: `UI_LoginTest`, `UI_LobbyTest`, `UI_StageSelectTest`, and `UI_CombatHudTest`, starting at login and returning to lobby through the UI route table.
- `DimensionBrawl > Validate Action Foundation Test Scene` validates required scene objects, component ownership, key references, shared target sensor wiring, promoted MaintenanceWorker enemy visual/Animator wiring, and reference-backed timing values from inside the Unity Editor.
- `DimensionBrawl > Validate Action Foundation Combat VFX Cues` validates the combat VFX cue profile, promoted `_Game` prefab paths, mesh-based `CombatVfxCueVisual` prefabs with no first-pass particle shards, ranged shot forward-travel cue values, player/enemy cue-player bindings, eight pattern cue overrides, and five elite signal cue overrides.
- `DimensionBrawl > Validate Action Foundation Test Scene` also validates player Animator wiring, root-motion-off state, promoted locomotion states, fast StopStep transition tuning, CombatGirl weapon socket binding, directional Quickshift dodge states, promoted five-hit attack states, game-owned clip paths, and the serialized action/camera timing values for the curated visual child.
- `DimensionBrawl > Reapply Action Foundation CombatGirl Materials` exists for the open-Editor stale-scene case: unpacked CombatGirl renderers can keep old material slots after `Assets > Refresh`, so this menu reassigns the open scene to `_Game` CombatGirl material assets and saves it.
- `DimensionBrawl > Reapply Action Foundation CombatGirl Weapon Sockets` restores hand-pinned `add_weapon_l` / `add_weapon_r` bindings, and `DimensionBrawl > Reapply Action Foundation StopStep Responsiveness` restores StopStep trim, speed, transition, and scene movement tuning.
- `DimensionBrawl > Reapply Action Foundation MaintenanceWorker Enemy Visual` rebuilds the promoted soldier visual, selected enemy animation clips, Animator Controller, scene references, disabled-placeholder state, and enemy hit-feedback renderer list if reimport or open-scene edits drift.
- `DimensionBrawl > Reapply Action Foundation Extended Enemy Patterns` rebuilds the extended general/elite pattern profile assets, deck assets, six authored extended scene samples, elite trait sample wiring, and twelve-candidate player target selector wiring.
- `DimensionBrawl > Reapply Action Foundation Enemy Role Decks` rebuilds the linear-run role deck assets and 12 general/elite role profiles under `EnemyRoles/`; `DimensionBrawl > Validate Action Foundation Enemy Role Decks` verifies role ids, pattern/deck references, distance bands, linear segment coverage, and general/elite ownership separation.
- `DimensionBrawl > Reapply Action Foundation Enemy Archetypes` rebuilds the role-to-presentation candidate assets under `EnemyArchetypes/` and can regenerate the promoted FORGE3D line-turret visual candidate from game-owned source assets; `DimensionBrawl > Validate Action Foundation Enemy Archetypes` verifies role coverage, promoted-only object references, turret gameplay-pending state, 3 promoted humanoid boss candidates, and future dragon boss candidate tracking.
- `DimensionBrawl > Reapply Action Foundation Enemy Prefab Candidates` promotes the reviewed close-punish soldier sample and the reviewed GeneralDeck/EliteDeck soldier samples into reusable `_Game` prefab candidates; `DimensionBrawl > Validate Action Foundation Enemy Prefab Candidates` verifies those prefabs are scene-free and game-owned, with close-punish mapped to `SciFiSoldier.Melee`, GeneralDeck mapped to `SciFiSoldier.Ranged`, and EliteDeck mapped to `SciFiSoldier.Elite`.
- `DimensionBrawl > Reapply Action Foundation Enemy Role Candidates` rebuilds the 12 role candidate profile/prefab pairs under `EnemyRoleCandidates/` and `RoleCandidates/`; `DimensionBrawl > Validate Action Foundation Enemy Role Candidates` verifies full 7-general/5-elite role coverage, role/archetype compatibility, promoted-only object refs, role-specific pattern/deck/elite trait wiring, local Animator/controller/clip assignments, role pattern and elite-signal animation parameter coverage, local VFX cue bindings, optional line-turret visual alternatives for line/backline roles, and scene-free target/camera ownership.
- `DimensionBrawl > Validate Action Foundation Stage Design Templates` verifies the authored first-pass linear stage segment/template assets under `StageDesign/`, including core segment coverage, route start/end shape, relief beats, non-relief enemy role slots, and promoted-only role references.
- `DimensionBrawl > Reapply Action Foundation Enemy Prefab Review Scene` rebuilds the one-enemy manual review scene from `ActionFoundationTest` plus the melee prefab candidate; `DimensionBrawl > Validate Action Foundation Enemy Prefab Review Scene` verifies the single prefab instance, player/enemy target candidates, encounter references, and camera binding.
- `DimensionBrawl > Reapply Action Foundation Enemy GeneralDeck Review Scene` rebuilds the one-enemy manual review scene from `ActionFoundationTest` plus the GeneralDeck prefab candidate; `DimensionBrawl > Validate Action Foundation Enemy GeneralDeck Review Scene` verifies the single prefab instance, retained GeneralDeck assignment, player/enemy target candidates, encounter references, and camera binding.
- `DimensionBrawl > Reapply Action Foundation Enemy Role Candidate Review Scene` rebuilds the all-role manual review scene from `ActionFoundationTest` plus the 12 role candidate prefabs; `DimensionBrawl > Validate Action Foundation Enemy Role Candidate Review Scene` verifies prefab instance links, scene-injected target candidates, camera bindings, first-enemy encounter reference, and authored review positions.
- `DimensionBrawl > Reapply Action Foundation Stage BreakGate Review Scene` rebuilds the `S1-1 Break Gate` route-composition scene from `ActionFoundationTest` plus the already-authored role candidate prefabs; `DimensionBrawl > Validate Action Foundation Stage BreakGate Review Scene` verifies the route template, prefab instance links, scene-injected target candidates, camera bindings, final-enemy encounter reference, and authored route positions.
- Unity batchmode validation passes with `Action foundation test scene validation passed.` after closing the open Editor instance.
- `ActionFoundationPlayModeTests` includes coverage for role candidate profiles/prefabs in addition to player movement, run-start/turn animation routing, stop-settle release, fast-window `StopStep` Animator routing, CombatGirl weapon socket alignment, directional dodge movement, no-input backward dodge, five-hit timed-buffer combo routing, basic attack damage without global slow motion, shared target sensing/team rules, twelve authored target candidates, promoted MaintenanceWorker enemy visual/Animator wiring, pattern-specific enemy animation trigger data, enemy Attack/Hit/Death animation requests including fatal-damage direct death routing and grounded death bounds, enemy windup telegraph presentation, `LinePressure`, `FanPressure`, ranged soldier projectile cue forward-travel values, authored `TrainingDeck`, extended general/elite pattern data and scene samples, linear-run enemy role profile coverage, role-to-archetype presentation candidate coverage, role candidate local Animator/controller/clip and trigger-parameter coverage, scene-free melee soldier prefab candidate mapping, scene-free GeneralDeck prefab candidate data with distinct `SciFiSoldier_01` rifle presentation and promoted assault-rifle mesh, close-punish and GeneralDeck prefab review scene wiring, elite signal animation trigger data, elite damage modification, aura receiver protection, summon signal activation, win/fail states, dodge tint feedback, short action-camera cue cleanup/FOV widening, independent camera orbit, and the lower close-rear camera preset. Batchmode PlayMode test runs should omit `-quit` so `-runTests` can write results before Unity exits.
- Manual open-Editor inspection confirmed the player placeholder visibly flashes/tints when dodging with `Left Shift`.
- Raw imported asset packs are ignored.
- `_Game/Art` folder structure exists for curated game-ready assets.
- `_Game/Art/VFX` exists as the curated destination for selected combat-ready VFX promoted out of local raw packs.
- `_Game/Art/Environment` is the curated destination for selected stage-ready environment assets. Do not commit raw source-pack texture originals there by default; promote game-ready texture copies or use a deliberate asset-depot decision first.
- For demo-scene-quality stage work, promote coherent art layers rather than tiny samples: lighting/postprocess, terrain, water, background silhouettes, vegetation density, particles, wind, and performance helpers should be considered together while still avoiding direct `_Imported` references.
- Project governance docs now define AI limits, folder ownership, code style, architecture boundaries, session workflow, and review checks.
- Current in-game cutscene rule: use Cinemachine/Timeline as a Unity-side production layer for short ARPG cut-ins and character scenes; every cutscene cue must declare shot type, duration, gameplay lock, time-scale behavior, signal events, camera handoff, and return-to-gameplay target before implementation.
- Current cutscene reference evidence rule: reference games did not yield public Cinemachine/Timeline source assets; use their public mechanic data, observed camera grammar, and public data-mirror field shapes only as schema/pattern evidence for our own Unity implementation.

## Current Art Storage Note

- Git LFS is not the active solution while the account budget is blocked.
- Unity Version Control is the preferred near-term art depot candidate for raw stage art because it is Unity-oriented and handles large binary workflows more naturally than the gameplay Git repository.
- Perforce remains a professional alternative if hosting/admin overhead is acceptable.

## Local Assets

Raw packs currently staged only as local files:

- `Assets/_Imported/AssetStore/CombatGirlsCharacterPack`
- `Assets/_Imported/AssetStore/Animation`
- `Assets/_Imported/AssetStore/Protofactor`
- `Assets/_Imported/AssetStore/HEROIC FANTASY CREATURES FULL PACK VOL3`
- `Assets/_Imported/AssetStore/VFX`
- `Assets/_Imported/AssetStore/FORGE3D`

These must not be committed directly.

- Boss-barrage visual polish now keeps the promoted Magic Missiles cue assets for summon/boss state reads but clamps their gameplay-scale size, alpha, lifetime, loop/prewarm behavior, and pressure-screen wash so the running frame still shows boss/summon/projectile positions. `ActionFoundationBossBarrageVisualSmokeTests` now includes a bright low-saturation pixel guard in addition to actor/cue-count checks.
- The result/follow-up cue pass keeps the promoted Magic Missiles cue assets active, uses compact profile scale/lifetime values for `PlayerDamaged`, `PlayerCritical`, `EliteSummonSignal`, `EliteShieldSignal`, `SummonFollowupWindow`, `SummonBlockOpportunity`, `SummonFollowupMissed`, `PocketCleared`, and `PocketFailed`, reduces the `PF_SummonSlot1EntryCue_MagicCircle` ArcaneCircle overlay, lowers all summon tier entry `CueScale`/`CueLifetimeSeconds`, and moves `PlayerCritical`/`PocketFailed` from the pale death-burst asset to the promoted light-impact asset while the existing fail accent supplies the red close-punish read. Death-burst remains available for enemy death and follow-up misses, while player-state, summon-entry, pressure-screen, and result-circle cues stay small enough that they do not hide boss/summon silhouettes during normal, clear, or fail result frames.
- The boss-barrage review result/readability tuning keeps clear/fail as a readable screen-edge beat without washing the full frame: the result screen cue now uses `0.15` full-screen alpha, `0.36` edge alpha, and `132px` edge thickness; pocket clear/fail world VFX intensities are `1.24`/`1.36` with a `1.22` fail accent; the clear world cue profile remains very compact. Summon pressure-screen visuals keep their gameplay intercept radius but render at a `0.22` visual radius scale with reduced activation/intercept cue intensities, so the 3x summon actors remain dominant while shield domes no longer cover the whole play read.
- The current boss-barrage review balance uses a shorter demo boss HP budget: the authored boss proxy is `420` HP, basic ranged fire deals `28` per shot at a `0.24s` cadence with longer `24m/s` / `1.75s` travel and wider far-boss aim assist, `Skill1` deals `84/104/128`, and `S1 ShieldBreaker`, `S2 BacklineMarksman`, and `S3 VanguardCommander` have raised projectile/DPS values while preserving their role order. Basic fire should visibly contribute while held on the far boss, but Skill1/summon pressure remains the stronger tactical payoff. Confirmed Skill1 follow-up hits also use a longer, stronger screen/VFX cue so the boss HP shift reads in short mobile capture. This is a one-pocket readability tuning pass, not a production boss kill-curve commitment.
- Pocket clear/fail now dismisses active player summon pressure screens, so leftover shield domes do not obscure the result cue while the authored summon actor/projectile flow remains intact during combat.
- Combat VFX cue prefabs now carry authored game-owned audio resources for rifle muzzle fire, ranged projectile impact, summon signal, summon block opportunity, and summon follow-up window cues. `ActionFoundationCombatVfxSetup` and the boss-barrage scene validator both require those audio resources, and the boss-barrage reapply path restores `SummonEnergyVfxCuePresenter` plus explicit pocket clear/fail VFX anchors so EN state, follow-up, and result reads do not silently drop after scene regeneration.

## Next Safe Step

Use `Assets/_Game/DesignDocs/COMBAT_V1_SPEC.md`, `Assets/_Game/DesignDocs/ACTION_FEEL_TARGETS.md`, `Assets/_Game/DesignDocs/ACTION_FOUNDATION_OWNERSHIP.md`, `Assets/_Game/DesignDocs/SUMMON_SYSTEM_REFERENCE_RESEARCH.md`, `Assets/_Game/DesignDocs/CINEMACHINE_INGAME_CUTSCENE_REFERENCE_RESEARCH.md`, and `Assets/_Game/DesignDocs/LINEAR_STAGE_DESIGN_FOUNDATION.md` as implementation guardrails. The next safe step is to keep tuning the current `ActionFoundationBossBarrageLaneReview.unity` loop so the close-threat local defense read, forward-risk EN pacing, boss projectile pressure, tier-aware `SummonSlot1` payoff, and one-pocket clear/fail read all feel strong without adding full summon roster/economy, full boss phases, rewards, progression, stage-select UI, or production chapter art yet.

## Current Risk
- Unity may recreate project setting folders while packages are inspected. Do not commit package/project setting changes unless they are intentionally reviewed.
- Unity may briefly report stale or malformed `.meta` data while files are being promoted. Verify the final `.meta` GUIDs and refresh/reimport in the open Editor before assuming the asset files themselves are broken.
- If the already-open `ActionFoundationTest` scene shows a pink CombatGirl after material promotion, run `DimensionBrawl > Reapply Action Foundation CombatGirl Materials`; refresh alone does not always update unpacked scene renderer material slots.
- Unity batchmode reruns may be blocked by the Unity Licensing Client/headless editor state while Editor instances are open; use the open Editor validation menu for quick checks, or close Unity before a full batchmode rerun.
- The project direction has pivoted to fixed-rear boss-barrage + summon-first lane combat. Do not continue expanding melee combo, free-chase ARPG routes, or Spring Isles route dressing as the core product direction before the first boss-barrage lane, forward-risk summon-energy, local-defense attack, and `SummonSlot1` slice is playable.
- Player movement boundaries are not summon movement boundaries. Do not route summon/frontline actors through `ClampPlayerPosition`; author any summon side limits through summon role, pattern, or battlefield data instead.
- Future boss-pattern work should use the same data-first path as the current boss-barrage slice: collected reference and boss-pattern notes, including `C:\Ark` when relevant, then `BossBarragePatternProfile`-style data, authored projectile/VFX/camera cue prefabs, review-scene validation, and PlayMode tests. Add patterns one at a time instead of introducing a hidden full boss phase manager.
- Action feel is still a quality gate, but the first gate is now movement, fixed rear camera, projectile readability, forward-risk `EN LV1~LV3` energy, local-defense attack, dodge, tiered skill/summon spend, hit feedback, and one clear pocket result. Do not add larger systems to hide weakness in that loop.
- The current CombatGirl material pass is primary-color recovery only. Do not mass-promote original matcaps, masks, or the full asset-pack material stack without a separate reviewed slice.
- Current action camera cues are intentionally small normal-action cues. Do not expand them into boss, summon, ultimate, or full lock-on camera systems without a separate reviewed slice.
- Current elite behavior is a data-backed trait foundation, not a production encounter system. Do not turn `SummonPackage` into runtime spawning, make `AuraBuffer` search the scene for allies, or add boss phases without a separate reviewed slice.
- Current enemy role profiles are planning/authoring data, not an encounter runtime. Do not make them spawn enemies, load assets by path, or replace authored prefabs; use them to decide which enemy prefabs/variant assets should be authored next.
- Current linear stage templates are planning/authoring data, not a runtime route system. Do not make them spawn enemies, load prefabs, pay rewards, mutate themselves at runtime, or become a stage-select UI source before a separate reviewed slice exists.
- Current enemy archetype profiles are planning/authoring data, not prefab builders. Do not assign raw `_Imported` FORGE3D or dragon prefabs to them; promote selected parts into `_Game` through a reviewed slice first.
- The promoted FORGE3D line-turret asset is presentation-only. Do not treat it as a gameplay-ready turret, missile turret, projectile spawner, summon unit, or wave actor until a separate reviewed static-turret gameplay slice exists.
- The first melee soldier prefab candidate is authored content, not a spawning contract. Do not put ActionFoundationTest player targets, camera controllers, encounter state, wave logic, or summon behavior into enemy prefabs.
- The role candidate prefabs are authored review variants, not encounter spawns. Do not use them as an excuse to add a runtime prefab builder, wave manager, summon spawner, or hidden fallback selection path.
- The five-hit combo is the current reviewed chain. Do not wire `SS_Sp_Skill2`, `SS_Sp_Skill3`, or other special clips as hits 6-7 until manual visual review confirms they still read as normal basic-chain finishers.
- `StartRun`, `TurnLeft90`, and `TurnRight90` should stay on CombatGirl-owned clips with weapon attachment curves. The current `SS_StopStep` intentionally uses the reviewed One Hand Base `Run_A_F_To_Idle_InPlace` candidate for stronger stop weight transfer; sword/shield stability is handled by `CombatGirlWeaponSocketBinder`, but manual visual inspection is still required before treating the stop feel as final.
- Do not reintroduce normal-hit global slow motion from the reserved hit-stop data. Time-scale effects should wait for a reviewed perfect-dodge, counter, ultimate, or cue-bundle implementation.
