# Frontline Combat Structure Anchor

Last updated: 2026-06-28 KST

This document is the checkpoint for the Frontline combat pass. Read and update it before any major edit, batch run series, or milestone commit.

## Current Canonical Context

- Manual review lane: `ActionFoundationBossBarrageLaneReview.unity` remains the authored fixed-rear boss-barrage lane review scene from `CURRENT_STATE.md` and `COMBAT_V1_SPEC.md`.
- Manual review lane parity: `ActionFoundationBossBarrageLaneReview.unity` directly binds `DB_FrontlineWaveStage_MotivationReview.asset` on both the pocket owner and HUD, so the manual scene reads the same stage row as the policy evidence loop.
- Policy evidence loop: `ActionFoundationFrontlineCombatPolicyReportTests` writes policy comparison reports to `C:\tmp\DimensionBrawl-FrontlineCombatPolicyReport.md` and `.json`.
- Motivation review pocket: the Frontline motivation scene/profile proves the clean route can exist, but route existence is not enough. The player must feel why summon/frontline structure matters.
- North star: a small corridor standoff match where player/boss bodies do not cross the contested line, while projectiles, pressure actors, and summons create pressure across it.

## Priority Premise

This pass is not a concrete balance-only pass.

The target is structural combat feel proven through repeated batch simulations. Each change must answer an ArkData-grounded missing piece, then show measurable separation between poor policies and the intended route.

## 2026-06-27 Reread Correction

The user's correction was not "avoid one named mistake"; it was "do not lose the premise and get attached to whatever looks fixable." Balance values, VFX, cleanup, scene routing, and test rewrites are only valid when they serve the stage/wave/result structure and the combat-state grammar below.

The one-match user flow recovered from the prior thread is:

`dodge/fire to read pressure -> local close threat creates a first beat -> summon crosses the line to solve pressure the player cannot directly solve -> Skill1 confirms the opened state -> result names the stabilized pressure`

Current next work must therefore ask whether the playable pocket still lacks a structural decision, state transition, or result proof. Do not reduce the pass to "make numbers feel better" unless the report first proves which structural decision or state transition those numbers serve.

## ArkData Comparison Lens

- NIKKE-like stage data: stage -> wave group -> pressure slots -> spawn/path/range counts -> scenario/result/reward hooks. Use this as data discipline, not copied numbers.
- CombatPayload-like grammar: Trigger -> Target -> Effect -> Status/Hit -> Presentation. Avoid bool-only proof when a combat event should explain who did what to whom and what changed.
- PGR-like combat state grammar: QTE/state lock-unlock, buff-gated punish windows, hit response tiers, dodge/matrix-style timing, and presentation bridges. Use the grammar, not tutorial UI.
- Blue Archive EX reference from ArkData/SchaleDB: `students.json` exposes `Skills[].SkillType = ex`, per-student `Cost[]`, `Effects`, `Range`, `Duration`, and student `RegenCost`. The snapshot proves individual EX cost identity and effect shape, but it does not expose an EX-skill `Cooldown`/`CoolTime` field. For Frontline summons, the canonical resource contract is therefore shared summon mana plus per-slot required mana plus per-slot cooldown as our narrow combat-choice layer, not separate per-slot gauges.

## Active Structural Axes

1. NoSummon and GunOnly should lose meaningful HP, windows, or state because they ignore frontline pressure.
2. Summon block -> counter window -> Skill1 punish should read as one combat-state loop with pressure, answer, and reward.
3. Hit reactions should communicate damage/status without repeated full-body interruption, except true break, lock, death, or major punish cases.
4. Enemy summons and pressure actors should create unattended tactical cost, not instant death and not timer-only bookkeeping.

## Prohibitions

- Do not switch canon to a new scene or generated stage unless the user explicitly asks.
- Do not add tutorial HUD/proxy overlays as the solution to missing combat grammar.
- Do not restore removed VFX/audio banks or broad presentation passes unless the active hypothesis explicitly requires a narrow reviewed cue.
- Do not build a full summon roster, boss phase manager, reward economy, stage select, or production chapter flow.
- Do not treat one passing clean-route test as proof of fun.
- Do not spend the pass on detailed damage curves, enemy roster growth, or cleanup unrelated to the combat structure gate.
- Do not chase old-thread drift patterns again: no new canonical scene, no proxy/tutorial HUD detour, no broad VFX/audio restoration, no old-commit rollback spiral, and no worktree cleanup rabbit hole unless it directly protects this combat pass.

## Prior-Thread Failure Patterns To Avoid

The repeated failure was not only a list of individual rule breaks. The larger failure was losing the premise and getting attached to whatever looked fixable next.

- Do not translate ArkData or PGR references into tutorial/proxy HUD first. They are combat-state grammar references: stage/wave/result structure, trigger/target/effect/status/presentation chains, lock/unlock timing, hit-response tiers, and punish-window proof.
- Do not make a new canonical scene, generated stage, broad setup tool, or cleanup pass to feel productive. The authored boss-barrage lane and the policy report own this pass unless the user explicitly redirects.
- Do not restore broad temporary VFX/audio or ambient loops as a feel fix. Presentation changes must be narrow, reviewed, and tied to a measured combat-state gap.
- Do not chase old commits or wide revert spirals. Inspect the active drift, protect user changes, and revert only the smallest current mistake if needed.
- Do not claim "fun" from route existence, copy changes, HUD text, or one passing scripted test. Fun progress in this pass means policy metrics separate bad routes, partial answers, complete answers, and recovery routes in the expected direction.
- Do not assume which manual scene or visual layer the user is seeing. Verify the canonical scene, scene setting, and runtime readout before concluding.
- Do not clean unrelated generators, stale assets, or worktree noise unless they directly block this combat-structure pass.

Before each next implementation edit, write the answer to these three questions in the work note or this document:

- What ArkData/CombatPayload/PGR missing piece is being introduced?
- Which structural axis and which report metric will prove or reject it?
- Which prior-thread drift pattern could this accidentally repeat?

## Simulation Gate

Every attempt should compare at least these policies when practical:

- NoInput or NoSummon
- GunOnly
- IntendedRoute
- LateSummon

Track enough metrics to prove route/pressure separation:

- player HP lost
- boss damage
- clean route result and route stability
- summon blocks or frontline stabilizations
- unanswered pressure hits
- counter window opens/uses/misses
- Skill1 punish confirmations
- enemy pressure actor reach/attack/control cost
- hit-lock or interruption counts, separated by minor hit versus break/major hit

## Before Major Edit Checklist

- What ArkData missing piece is this change introducing?
- Which of the four structural axes does it serve?
- Which metric should improve or separate after simulation?
- Which current canonical scene/profile/test owns the evidence?
- Which prohibition could this accidentally violate?
- Is this narrow enough for the code style and ownership docs?

## Decision Log

- 2026-06-26: Anchor created after reading the prior thread failures, project docs, and recent commits. The main correction is to preserve the big premise: ArkData-grounded structure and simulation-proven combat feel, not isolated balance tuning or side-system work.
- 2026-06-26: Expanded `ActionFoundationFrontlineCombatPolicyReportTests` from four policies to six. Added `MissedFollowupCounterRecovery` to prove the missed follow-up -> counter wave -> ally hold -> final Skill1 loop, and `BossScreenBlockedFollowup` to prove enemy boss-screen pressure can block Skill1 and leave the route in a recovered-but-unfinished state.
- 2026-06-26 batch evidence: NoSummonNoFire HP lost 57.0 / stability 0%; GunOnly HP lost 39.0 / boss damage 70 / stability 0%; IntendedRoute HP lost 0 / boss damage 208 / stability 71%; LateSummon HP lost 3.6 / boss damage 208 / stability 63%; MissedFollowupCounterRecovery clears as `CounterRecoveryClear` with `followup_miss`, final window opened, Skill1 hits 3, boss damage 384; BossScreenBlockedFollowup records `boss_screen`, blocks 2 Skill1 projectiles, records boss-screen follow-up block true, opens final window, but remains `Running` with 0 boss damage.
- 2026-06-26 resolved gap: BossScreenBlockedFollowup now reports counter source `boss_screen` when boss-screen pressure blocks Skill1 projectiles. This is the first narrow CombatPayload grammar correction in the pass: the Trigger source is separated before any balance tuning.
- 2026-06-26 resolved authored-scene gap: `PocketReadsBossSummonBlockAsFollowupFailure` now treats boss-screen Skill1 interception as a real `boss_screen` counter trigger, keeps the blocked follow-up readable for camera/VFX/objective cues, and no longer collapses into an instant recovery window.
- 2026-06-26 structural decision: counter-wave ally hold must come from a fresh SummonSlot1 response after the counter trigger, not from a stale summon already present before the miss/block. This preserves the PGR-like lock -> answer -> unlock grammar and keeps boss-screen blocks in `answer_counter` until the player rebuilds the summon answer.
- 2026-06-26 batch evidence after fresh-answer gate: NoSummonNoFire HP lost 28.0 / stability 0%; GunOnly HP lost 18.8 / boss damage 70 / stability 0%; IntendedRoute HP lost 0 / boss damage 208 / stability 71%; LateSummon HP lost 8.0 / boss damage 208 / stability 63%; MissedFollowupCounterRecovery clears as `CounterRecoveryClear` with `followup_miss`, fresh summon count 2, final window opened, boss damage 208; BossScreenBlockedFollowup records `boss_screen`, blocks 2 Skill1 projectiles, stays `Running` with `answer_counter` pending and 0 boss damage until a fresh counter answer is supplied.
- 2026-06-26 batch evidence extension: Added `BossScreenBlockCounterRecovery` to prove the boss-screen block branch is not a dead end. Latest report separates `BossScreenBlockedFollowup` as `Running` / `boss_screen` / `answer_counter` pending from `BossScreenBlockCounterRecovery` as `CounterRecoveryClear` / `boss_screen` / final window opened / boss damage 208 after a fresh SummonSlot1 answer. This closes the Trigger -> Status lock -> fresh Answer -> final Skill1 punish loop for both follow-up miss and boss-screen block sources.
- 2026-06-26 measurement correction: Added `Frontline Clash Cost` metrics to the policy report and added `BossScreenIgnoredNoRecovery` as an explicit no-answer counter policy. The first run exposed an axis-4 gap: enemy frontline presence and summon-vs-summon clashes were measurable, but ignored boss-screen pressure still produced 0 body hits / 0 player damage because stale ally summons physically held pressure even after the route required a fresh answer.
- 2026-06-26 structural correction: Changed `DB_SummonSlot1_ShieldBreaker` actor lifetimes from infinite to timed frontline holds (LV1 4.4s / LV2 5.2s / LV3 6.2s), and changed `DB_BossSummonPressure_SummonCaller` from slow walk pressure into readable BodyRush pressure (move speeds LV1 2.35 / LV2 2.15 / LV3 1.95 with larger engage radii). ArkData reason: enemy pressure was previously `Calling/ObjectCreate` presence without enough `Dash/BodyRush` consequence. PGR reason: stale summon bodies must not undermine the lock -> fresh answer -> unlock rule.
- 2026-06-26 batch evidence after BodyRush correction: NoSummonNoFire now records HP lost 69.1, enemy body hits 9, enemy clash damage 12.1; GunOnly remains `Running` with boss damage 70 and HP lost 30.6; IntendedRoute remains `CleanFollowupClear` with HP lost 0, boss damage 208, and no enemy body hits; BossScreenIgnoredNoRecovery now records HP lost 10.8, enemy clashes 18, body hits 8, body damage 10.8; BossScreenBlockCounterRecovery still clears with boss damage 104.0, 1 Skill1 hit, and only 1 body hit / 1.3 HP lost before fresh summon recovery. This is the first verified separation where ignoring enemy pressure actors creates a physical tactical cost, while the recovery route converts that pressure into summon clashes and a final Skill1 punish.
- 2026-06-26 active hypothesis: Axis 2 is still thin. `BossScreenBlockCounterRecovery` proves lock -> fresh answer -> final window, but the payoff is only 104 boss damage / 1 Skill1 hit because a successful counter answer can remain in the Critical band and compress the final punish to 0.65x. ArkData/CombatPayload gap: Trigger and Status are present, but Effect/Reward is weak. PGR gap: an earned unlock should escape Critical enough to read as a punish window. Next narrow attempt: raise the counter-wave stabilize route bonus only if simulation shows recovery payoff improves without making ignored pressure safe.
- 2026-06-26 batch evidence after counter-unlock payoff change: raised `counterWaveStabilizeRouteBonus01` from 0.14 to 0.22 so a fresh counter answer can escape Critical compression. Focused policy report with new assertions passed: `BossScreenBlockCounterRecovery` now opens the final window at x0.85, lands at least 2 Skill1 hits, and deals clean-followup-or-better boss damage (latest 384.0, repeated run 208.0) while `BossScreenIgnoredNoRecovery` stays `Running` with 9 body hits / 12.1 HP lost and 0 boss damage. `NoSummonNoFire` and `GunOnly` still end at 0% route stability, so the route separation remains intact.
- 2026-06-26 focused authored-scene evidence: `FrontlineMissedFollowupRecordsCounterWaveBeforeRecovery` now treats the fresh counter answer as a real recovery payoff, allowing route stability to recover past the pre-answer pressure state, locking the screen cue at/above the unstable window threshold, and recording `Record A: Counter recovery` at `pressure 62%`.
- 2026-06-26 active hypothesis: Axis 3 should be verified at the policy-report layer before adding more presentation. CombatPayload's useful gap is `Status/Hit -> Presentation`: the current runtime already tags summon-vs-summon and player-body pressure as `FlashOnly` / no control lock, while major damage can still use interrupt or hard-lock. PGR reference data also keeps hit effect ids and lock/state candidates separate. Next narrow attempt: extend the Frontline policy report with summon damage-flash, full-body hit-trigger, suppression, and non-locking-vs-locking cue counts so minor frontline clashes prove they are readable without repeated full-body interruption.
- 2026-06-26 batch evidence after hit-reaction reporting: extended `ActionFoundationFrontlineCombatPolicyReportTests` with `Hit Reaction Presentation` metrics. Latest policy report passed with `BossScreenBlockCounterRecovery` producing 34 summon damage flashes, 0 full-body hit reactions, and 34 non-locking damage cues; `BossScreenIgnoredNoRecovery` produced 21 summon damage flashes and 0 full-body hit reactions while still costing 9 body hits / 12.1 HP lost. Axis 3 is now measured in the same policy loop as axis 4 instead of relying on a separate presenter unit test.
- 2026-06-26 active hypothesis: Axis 3 still needs the positive contrast: minor pressure damage is non-locking, but true punish hits should still register as control-lock / full-body-eligible damage events. CombatPayload reason: `ProjectileOrHitEvent` should carry damage response policy before presentation; PGR reason: hit effects and lock/state candidates are separate rows, so "no repeated full-body reaction" must not erase major hit reads. Next narrow attempt: add damage-response policy counts for player, boss, and close threat to the same policy report, proving gun/basic pressure stays non-locking while Skill1/final punish hits register as locking.
- 2026-06-26 batch evidence after damage-response reporting: added `Damage Response Policy` counts to the policy report. Latest run passed with no-action player pressure at `15/0/0` non-lock/lock/full-body-eligible, gun-only boss chip at `5/0/0`, intended Skill1 boss hits at `0/2/2`, and boss-screen recovery boss hits at `0/2/2`. This gives Axis 3 both sides of the contract: routine pressure and summon clashes stay non-locking, while true Skill1 punish hits remain locking/full-body-eligible.
- 2026-06-26 active hypothesis: Axis 3 still has a `Status/Hit -> Presentation` bridge gap in the policy report. Damage-response counts prove Skill1/final punish hits are locking/full-body-eligible, but the report does not yet prove the existing `Followup.Hit` screen cue, follow-up hit camera cue, and promoted VFX cue fire for those same routes. CombatPayload reason: Presentation should be measured after Status/Hit, not inferred from damage. PGR reason: major hit state and hit-effect presentation are separate rows that must both fire. Next narrow attempt: bind the existing follow-up presentation cue counters into the batch policy report without adding a new presentation system.
- 2026-06-26 batch evidence after follow-up presentation reporting: added `Follow-up Presentation Bridge` counts to the policy report. Latest run passed with gun-only follow-up hit cues at screen/camera/VFX `0/0/0`, intended route at `1/1/1`, boss-screen recovery at `1/1/1`, and boss-screen blocked route split as miss cues `1/1/1` but hit cues `0/0/0`. This closes the Axis 3 policy-report chain for true punish hits: Status/Hit (`0/2/2` or `0/3/3` boss lock/full-body counts) now has matching Presentation proof through screen, camera, and promoted VFX cue bridges, while non-follow-up chip remains excluded.
- 2026-06-26 active hypothesis: Axis 3 still needs the major-hit cinematic/frame bridge measured in the same policy loop. The report now proves `Followup.Hit` screen/camera/VFX cues, but PGR-like punish feel also depends on the short cinematic/frame overlay path owned by `BossBarragePocketCameraCueBridge` -> `ActionCinematicCueDirector` -> `ActionCinematicSequenceBridge`. CombatPayload reason: a Presentation row can have multiple adapters, and the major hit should prove the stronger cue path only on true punish routes. Next narrow attempt: add cinematic director/sequence bridge counts for follow-up window, hit, and miss without changing cue content.
- 2026-06-26 batch evidence after cinematic bridge reporting: added `Follow-up Cinematic Bridge` counts and confirmed the current canonical setup keeps `ActionCinematicCueDirector` / sequence playback disabled, so all follow-up cinematic director/sequence counts remain `0/0/0`. This is not a failed route payoff; it means current Frontline feel must be judged through the verified screen/camera/VFX bridge until a dedicated cinematic pass changes setup. During this check, the policy runner also exposed a nondeterministic recovery payoff race: yielding a frame before manual Skill1 impact let physics/pressure screens sometimes collapse `BossScreenBlockCounterRecovery` to 1 hit / 104 damage. The runner now applies the scripted policy impact immediately after Skill1 fire for follow-up-confirm routes. Two focused batch runs passed after the fix: boss-screen recovery landed 3 hits / 384 damage, then 2 hits / 208 damage, both clean-followup-or-better while cinematic counts stayed intentionally zero.
- 2026-06-26 active hypothesis: the policy report still lacked ArkData-style pressure concentration evidence. HP loss, route stability, and body hits prove whether policies win or lose, but not whether the pocket has readable burst pressure, relief spacing, route dominance, and unanswered-vs-answered pressure burden. ArkData reason: stage pressure should be judged through peak-window share, top-window concentration, dominant route/state burden, and time-to-relief shape before more tuning. Next narrow attempt: add 3-second pocket pressure-window metrics to the existing batch report without changing runtime balance.
- 2026-06-26 batch evidence after pressure-shape reporting: added `ArkData Pressure Shape` counts to the policy report and JSON. Two repeated PlayMode batch runs passed. Latest run records no-action peak/top3 pressure share `24%/70%` with dominant `enemy_only 70%`, intended route `44%/100%` with unanswered burden `33%`, ignored boss-screen `22%/57%` with dominant `enemy_only 55%` and unanswered burden `66%`, and boss-screen recovery `23%/64%` with dominant `contested 37%` plus a `6.0s` relief gap. This means the next feel decisions can compare pressure concentration and answer burden, not only damage curves.
- 2026-06-26 measurement correction: the first pressure-shape report mixed ArkData-like potential pressure slots with effective combat-state pressure after the player answered. `BossBarragePocketReviewOwner` already drops route drain during the summon follow-up window, so the report now samples `CurrentRouteStabilityDrainPerSecond` and fills trailing zero-pressure windows before judging relief. This keeps future tuning tied to CombatPayload/PGR effective Status pressure, not raw slot existence.
- 2026-06-26 batch evidence after effective-pressure correction: three focused PlayMode report runs passed. Latest report records no-action effective peak/top3 `24%/70%`, intended route `55%/100%` with a `3.0s` relief gap and unanswered burden `36%`, ignored boss-screen unanswered burden `72%` with no relief gap, and boss-screen counter recovery with a `6.0s` relief gap after the fresh summon answer. The pressure-shape gate now verifies that clean and recovered routes actually create relief, while ignored boss-screen pressure remains costly.
- 2026-06-26 active hypothesis after cadence reporting: `Lock/Unlock Cadence` exposed a structural Axis 2 gap. Boss-screen recovery did eventually work, but the counter trigger -> fresh summon answer took `6.5s` because the blocked Skill1 spent the player's summon/skill resource and recovery had to wait on passive recharge. ArkData/CombatPayload reason: the counter trigger had Status and Presentation, but its Effect did not grant the answer resource. PGR reason: a lock -> QTE answer should unlock the response promptly, not hide the answer behind passive refarm.
- 2026-06-26 structural correction and batch evidence after counter-answer pulse: added `counterWaveAnswerEnergyPulseOverride` to the canonical Frontline stage profile and let `BossBarragePocketReviewOwner` grant that pulse once when a counter wave is first observed. Latest repeated policy reports passed with answer pulse `105`; boss-screen recovery now reads counter->answer `0.0s`, answer->stable `0.4s`, stable->final `0.0s`, final->hit `0.1s`, clears in `9.1s` with `0.0` HP lost / `208.0` boss damage / `78%` stability, while ignored boss-screen pressure remains `Running` for `16.8s` with `8` body hits and `10.8` HP lost. The scene contract test for the new profile field also passed.
- 2026-06-26 active hypothesis after answer-pulse verification: Axis 2 still had a possible "perfect script" blind spot. Clean/recovery routes proved immediate Skill1 confirmation, but ArkData/PGR-style state windows should tolerate a short human response margin. CombatPayload reason: the Trigger -> Status -> Effect loop must expose a measurable punish-window duration, not only a same-frame bool confirmation.
- 2026-06-26 batch evidence after punish-window margin reporting: added `IntendedDelayedFollowup` and `BossScreenDelayedCounterRecovery` to the policy report, plus a `Punish Window Margin` section and JSON fields for first/last follow-up window duration, hit delay, remaining time, and used share. Two focused PlayMode runs passed (`WindowMargin-3` and `WindowMargin-4`). Latest report records delayed clean route as `CleanFollowupClear` after `1.1s` with `1.1s` remaining, and delayed boss-screen recovery as `CounterRecoveryClear` after about `1.2s` with `1.1s` remaining. Clear-result pressure windows now include a trailing zero-pressure window, so relief remains visible even when a route clears exactly on a 3-second boundary.
- 2026-06-27 active hypothesis: the policy report still did not prove the V1 forward-risk EN premise. ArkData/Arknights pressure-shape reason: route decisions need risk/reward differential metrics, not only eventual summon route success. CombatPayload reason: the energy Effect that enables `SummonSlot1` must be measurable before the summon answer happens.
- 2026-06-27 batch evidence after forward-risk energy reporting: added `BacklineEnergyProbe`, `ForwardRiskEnergyProbe`, a `Forward-Risk Energy Split` markdown section, and JSON fields for LV1-ready time, target/average forward risk, average gain multiplier, and Back/Mid/Forward band dwell. Two focused PlayMode runs passed (`EnergyRisk-1` and `EnergyRisk-2`). Latest report records backline LV1 at `10.2s` / `x0.59` / BackSafety, forward-risk LV1 at `3.8s` / `x1.61` / ForwardRisk, making forward-risk about `2.7x` faster. This verifies the reward side of forward-risk EN, but it also exposes the next gap: the current policy loop does not yet directly prove forward-risk danger, because the forward probe reaches LV1 before accumulating body hits or explicit position-specific projectile cost.
- 2026-06-27 prior-thread drift audit: repeated mistakes came from making new canonical scenes, relying on HUD/tutorial/proxy presentation, restoring broad temporary VFX/audio, reverting too far into old commits, and cleaning unrelated generators instead of staying on the active combat structure question. Current guardrail: every next edit must name the ArkData/CombatPayload/PGR missing piece, the axis it serves, and the simulation metric that will prove or reject it.
- 2026-06-27 active hypothesis: forward-risk danger should be proven through existing boss barrage targeting geometry before any HP tuning. ArkData reason: stage pressure is spatial wave shape, not just damage curves. CombatPayload reason: `Target` geometry must be measured before claiming an `Effect` cost. PGR reason: forward commit should tighten the state window and dodge/read burden, not silently grant EN. Next narrow attempt: add backline-vs-forward barrage-shape probes to the policy report using the existing `BossBarrageEmitter.BuildPendingLaneTargetPreview` data.
- 2026-06-27 batch evidence after barrage-shape reporting: added `BacklineBarrageProbe`, `ForwardRiskBarrageProbe`, a `Forward-Risk Barrage Shape` markdown/JSON section, and assertions that forward-risk barrage geometry compresses around the player lane. Two focused PlayMode runs passed (`BarrageShape-2`, `BarrageShape-3`). Latest report records forward-risk EN still at `10.2s -> 3.8s` / `x0.59 -> x1.61`, while the active `LinePressure` barrage compresses avg lateral gap `2.79 -> 2.25`, nearest lane pressure `2.77 -> 2.19`, and density `0.29 -> 0.37`. Important gap retained: near-body projectile count remains `0/4 -> 0/4`, so direct position-specific hit danger is not yet proven.
- 2026-06-27 structural correction and batch evidence after direct-risk LinePressure: narrowed `DB_BossBarrage_LinePressure.forwardHalfSpread` from `3.0` to `1.4` and mirrored the authored setup/test values, so backline still keeps the rail away while forward-risk turns the same rail into a near-body dodge tax. Focused pattern test passed, and two policy report runs passed (`LinePressureDirect-1`, `LinePressureDirect-2`). Latest report records forward-risk barrage split as near-body `0/4 -> 1/4`, avg lateral gap `2.66 -> 1.23`, nearest pressure `2.65 -> 1.23`, density `0.31 -> 0.56`, while forward-risk EN remains `10.2s -> 3.8s` / `x0.59 -> x1.61`. This closes the first direct position-specific danger proof without adding HUD, new scene, or broad damage tuning.
- 2026-06-27 active hypothesis: the next danger proof must use the actual projectile simulation path, not a forced `TryApplyImpact` policy shortcut. ArkData reason: wave shape should become concrete lane/time cost. CombatPayload reason: `Target` geometry should flow into `Hit/Status` through runtime collision before claiming an `Effect`. PGR reason: forward commit should create a real dodge/read burden, not only a preview metric. Next narrow attempt: add backline-vs-forward physical barrage probes that let existing boss projectiles fly and record real player damage/hit results.
- 2026-06-27 rejected attempt: physical barrage probes exposed that `forwardHalfSpread 1.4` was still not enough. The report failed exactly where it should: backline physical barrage `0/4` hits and forward physical barrage `0/4` hits, despite the preview metric reading near-body `0/4 -> 1/4` and nearest lane pressure `2.65 -> 1.23`. Decision: keep the new physical probe and narrow only the LinePressure forward rail until preview compression becomes actual runtime `Hit/Status` cost.
- 2026-06-27 structural correction and batch evidence after physical barrage probe: added `BacklinePhysicalBarrageProbe` / `ForwardRiskPhysicalBarrageProbe` to the policy report and narrowed canonical `LinePressure.forwardHalfSpread` to `0.0` so forward-risk targeting becomes real runtime hit cost while backline stays safe. Failed midpoint `0.45` still produced `0/4 -> 0/4` physical hits, proving this was not just preview cosmetics. Repeated policy runs then passed with backline physical barrage `0/4` hits / `0.0` damage and forward physical barrage `3/4` hits / `30.0` damage. The same report keeps forward-risk EN at `10.2s -> 3.8s` / `x0.59 -> x1.61`, while preview shape reads near-body `0/4 -> 4/4`, nearest `2.53 -> 0.47`, density `0.32 -> 0.95`. Focused LinePressure formula test also passed. This closes the first Target -> Hit/Status proof for forward-risk danger without adding HUD, a new scene, or broad damage tuning.
- 2026-06-27 active hypothesis: Axis 2 should now be verified with the same physical standard. The clean route currently proves summon block through a manual `screen.TryIntercept` policy helper, while forward-risk danger now uses real flying projectiles. ArkData reason: the authored wave's `Hit` cost and the summon answer's `Block` result should share the same stage/wave lane path. CombatPayload reason: `Trigger -> Target -> Effect -> Status` should close through runtime collision for both damage and block. PGR reason: a QTE-like summon answer should visibly convert incoming danger into an immediate follow-up state. Next narrow attempt: add a forward-risk physical summon-block probe that lets the existing screen intercept real `LinePressure` projectiles and verifies player damage drops while the follow-up window opens.
- 2026-06-27 rejected physical-block probe attempts: first probe proved real projectile blocks but skipped the close-threat beat, so no follow-up window opened. The second probe opened the window but let the automatic boss pattern advance to `LayeredSalvo`, so the comparison no longer matched the forward-risk `LinePressure` danger proof. Decision: keep the physical block probe, restore the close-threat prerequisite, then re-queue the captured current pattern before firing the physical wave.
- 2026-06-27 batch evidence after physical summon-block probe: added `ForwardRiskPhysicalSummonBlockProbe` to the policy report. Repeated PlayMode runs passed with the same `LinePressure` runtime projectile path: unblocked forward-risk physical barrage recorded `4/4` hits / `40.0` damage, then `3/4` hits / `30.0` damage on repeat; the summon-block probe recorded `2` summon blocks, `0/4` player hits, `0.0` damage, and `0.0s` block -> follow-up window in both runs. This closes Axis 2 at the physical-path level: the same authored wave that punishes an unanswered forward-risk commit is converted by a summon answer into immediate counter-window state without HUD/proxy detours or a new canonical scene.
- 2026-06-27 active hypothesis: physical block now needs its positive punish half on the same runtime path. ArkData reason: the same pressure-slot row should express danger, answer, and reward, not split block proof from follow-up payoff. CombatPayload reason: `Block -> Status` must be followed by a real `Skill1 Hit -> Presentation` event, not only a direct `TryApplyImpact` helper. PGR reason: a QTE-like answer should become an immediate player-authored punish beat. Next narrow attempt: add a forward-risk physical summon-punish probe that waits for the real `LinePressure` block window, fires Skill1, lets the projectile travel, and records physical boss hit damage/cues.
- 2026-06-27 batch evidence after physical summon-punish probe: added `ForwardRiskPhysicalSummonPunishProbe` and shared the physical barrage result counter across the no-answer, block-only, and punish probes. Two focused PlayMode runs passed. Latest report records the same `LinePressure` wave as forward-risk unblocked `4/4` hits / `40.0` damage, block-only `2` summon blocks / `0/4` player hits / `0.0` damage / `0.0s` block -> window, and block+punish as `CleanFollowupClear` with `2` summon blocks, `0/4` player hits, `2` real Skill1 projectile hits, `335.9` boss damage, `0.4s` window -> hit, `2/2` boss lock/full-body-eligible damage events, and `Followup.Hit` screen/camera/VFX hit cues all present (`1/1/1`). This closes the same-row ArkData loop for Axis 2: danger, summon answer, state unlock, real projectile punish, and presentation cue are now all measured without manual block/impact shortcuts.
- 2026-06-27 active hypothesis: Axis 1 should not change the canonical fail rule from HP to route stability collapse. Existing scene tests and profile copy state that HP is the fail state, while route stability is pressure/read burden. ArkData result-hook gap is therefore not "fail at 0% stability"; it is whether NoSummon/GunOnly eventually reach the existing HP fail state, and how long that takes compared with clean physical summon-punish. Next narrow attempt: add long survival-limit NoSummon/GunOnly probes that run until player down, boss down, or a cap, then report time-to-player-down/boss-down without changing failure rules.
- 2026-06-27 batch evidence after long survival-limit probes: added `NoSummonSurvivalLimit` and `GunOnlySurvivalLimit` with `45.0s` caps and player/boss down timers. Repeated PlayMode runs passed with assertions. Latest report records no-summon as `PlayerDownFail` at `21.9s`, `120.5` HP lost, boss down `-`; gun-only as `PlayerDownFail` at `16.8s`, `120.2` HP lost, `154.0` boss damage, boss down `-`. This closes Axis 1 without changing the canonical HP fail rule: ignoring summon/frontline structure eventually reaches the existing failure state, and basic fire cannot replace the summon route before the player dies.
- 2026-06-27 report guardrail: added a `Structural Gate Summary` at the top of the batch report. It reduces drift by forcing the next decision to read Axis 1 bad-route failure, Axis 2 physical block -> Skill1 loop, Axis 3 hit/presentation split, Axis 4 enemy pressure actor cost, and the physical clean route reference before scanning detailed tables. Latest batch shows all four axes as `PASS`; future tuning should preserve these gates unless a new ArkData-grounded hypothesis intentionally changes one.
- 2026-06-27 report guardrail extension: add an `ArkData Coverage Summary` before the structural gates. This keeps the big premise visible: NIKKE-style stage-result runtime is only partial because reward/item persistence is out of this V1 combat slice; CombatPayload coverage must be proven through runtime Target -> Hit, Block -> Status, Skill1 Hit -> Presentation; PGR coverage must stay at lock/unlock, delayed punish margin, and hit-response tiers rather than tutorial HUD flow. Use this summary before adding any new combat change.
- 2026-06-27 active hypothesis: Axis 2 needs boss payoff attribution before any damage tuning. The physical block-only probe currently shows meaningful boss damage even without Skill1, so the report must separate boss damage by `DamageInfo.SourceTeam` (`Player`, `AllySummon`, enemy/unknown). ArkData/CombatPayload reason: `Effect/Reward` should name who produced the payoff. PGR reason: a QTE-like block answer can help, but the player-authored Skill1 punish should remain visible as a distinct payoff source.
- 2026-06-27 rejected midpoint: reducing `DB_SummonSlot1_ShieldBreaker` actor DPS from `48/64/84` to `32/44/58` lowered block-only summon boss damage but still left the physical punish payoff at player/summon `208.0/127.9`, only `62%` player share. The direction was right, but the shield-breaker auto-DPS still competed with the player-authored Skill1 reward.
- 2026-06-27 rejected partial fix: reducing `DB_SummonSlot1_ShieldBreaker` actor DPS further to `24/32/42` dropped block-only boss damage to `169.9`, but physical punish stayed player/summon `208.0/127.9` and `62%` player share because the large Slot1 assist-bolt payoff, not just actor DPS, was competing with Skill1. This exposed a better CombatPayload split: ShieldBreaker's `Block -> Status` can keep helper pressure, but its immediate `Effect` damage must not rival the player-authored punish.
- 2026-06-27 narrow attempt: reduce ShieldBreaker assist projectile damage from `78/94/116` to `56/72/92`, keep it between Vanguard defense and Marksman damage, keep counter damage at `0.32x`, keep actor DPS `24/32/42`, and sync profile/generated setup/both canonical scenes with timed actor lifetimes `4.4/5.2/6.2`. Expected simulation result: block-only summon boss damage stays below player Skill1 payoff, physical summon-punish reaches at least `68%` player boss-damage share, and Axis 1/3/4 gates remain intact.
- 2026-06-27 batch evidence after ShieldBreaker payoff split: repeated policy reports passed (`BossAttribution-4`, `BossAttribution-5`). Latest report records physical block-only boss damage player/summon `0.0/133.8`, physical summon-punish `208.0/91.8`, `69%` player boss-damage share, `CleanFollowupClear`, `0/4` player barrage hits, `2` Skill1 hits, and follow-up hit cues `1/1/1`. Structural gates remain `PASS`: bad routes still reach HP fail, forward-risk barrage still hits `3/4` unblocked, hit response still separates routine non-locking damage from Skill1 locks, and ignored boss-screen pressure still costs `9` body hits / `12.1` HP while recovery costs `0.0` HP. Targeted role-budget test `SupportSummonRoleBudgetKeepsDamageScreenAndTankReadsDistinct` also passed, so ShieldBreaker stays between Marksman damage and Vanguard defense instead of becoming the damage summon.
- 2026-06-27 setup drift correction: attempting `ValidateBossBarrageLaneReviewSceneMenu` exposed stale generated setup expectations for `DB_BossSummonPressure_SummonCaller` BodyRush values. Synced setup generation/validation and both canonical scene serializations to the already-canonical profile values: move speed `2.35/2.15/1.95`, engage radius `1.25/1.35/1.5`. Follow-up policy report (`BossAttribution-6`) still passed with the same payoff split: block-only `0.0/133.8`, physical punish `208.0/91.8`, player share `69%`, all structural gates `PASS`. The broad editor validation then stopped on missing `BossBarrageLaneReview_AmbientAudio`; this is outside the current combat-structure hypothesis and should not be fixed by restoring broad audio as part of this pass.
- 2026-06-27 stage-result hook guardrail: added `Stage Result Hook Contract` to the policy report without creating reward economy or stage-select progression. The report now classifies committed result records as `failure_analysis`, `clean_survival`, or `counter_recovery`, marks them as `review-only analysis`, and keeps short running routes as `no committed result`. Two repeated PlayMode batch runs passed. Latest repeat records NoSummon/GunOnly survival failures as `PlayerDownFail` with `failure_analysis` hooks at `20.3s` / `23.5s`, physical summon-punish as `CleanFollowupClear` with `clean_survival`, and boss-screen recovery as `CounterRecoveryClear` with `counter_recovery`. NIKKE stage-result coverage remains deliberately `PARTIAL` because reward/item persistence and campaign clear are outside this V1 combat slice.
- 2026-06-27 active hypothesis: Axis 2 still had a possible false-positive. Physical summon block proved the same `LinePressure` projectile path could be converted into a follow-up window, but the report did not yet prove that block alone is incomplete. ArkData reason: the same pressure slot must distinguish `answer started` from `result committed`. CombatPayload reason: `Block -> Status` needs either `Skill1 Hit -> Presentation` or `NoHit -> Counter`, not a bool-only success. PGR reason: a QTE-like state window should relock or counter if the punish input is missed.
- 2026-06-27 batch evidence after unconfirmed follow-up reporting: added `ForwardRiskPhysicalSummonNoPunishProbe`, `Unconfirmed Follow-up Cost`, and updated the top summaries to require the no-punish branch. Repeated PlayMode policy reports passed. Latest repeat records unblocked forward-risk physical barrage as `4/4` hits, block-only as `2` summon blocks / `0/4` player hits / `0.0s` block -> window, no-punish as `Running` with follow-up hit/miss `0/1`, `1` counter wave, `75%` unanswered burden, `0` Skill1 hits, `0` result records, and boss damage player/summon `0.0/133.8`. Physical summon-punish remains `CleanFollowupClear` with follow-up hit/miss `1/0`, `0%` unanswered burden, `2` Skill1 hits, `1` result record, and boss damage player/summon `208.0/91.8`. This locks the Axis 2 read: summon block opens the state, but player-authored Skill1 confirmation is what commits the result.
- 2026-06-27 active hypothesis: Axis 2 still had a `Block -> Presentation` hole. The physical summon block opened the follow-up state and pressure-screen flash/VFX fired, but the top CombatPayload summary exposed camera block cues as `0/2/2`. ArkData reason: the same pressure-slot answer should carry both state and readable presentation. CombatPayload reason: `Block -> Status` cannot imply `Presentation`. PGR reason: a QTE-like block beat needs an immediate perceptual hit-stop/camera read, not only a later punish cue.
- 2026-06-27 batch evidence after summon-block presentation bridge: added `Summon Block Presentation Bridge` metrics/assertions to the policy report, exposed `ActionCameraCueDriver.RequestSummonPressureBlockCue`, and routed `BossBarragePocketCameraCueBridge` from the existing SummonSlot1 block event into that narrow camera cue without enabling the full action camera driver. Focused PlayMode report and two repeats passed. Latest repeat records CombatPayload runtime pipeline `PASS`: forward-risk `Block->Status/Presentation` has `2` real projectile blocks, `0.0s` block -> window, and cues `2/2/2`; the section rows show block-only, no-punish, physical punish, and intended route all at camera/opportunity `2/1` with pressure-screen flash/VFX present. This closes the immediate block-read gap while preserving the existing conclusion that Skill1 confirmation, not block alone, commits the result.
- 2026-06-27 setup drift correction: aligned `ActionFoundationBossBarrageLaneReviewSetup` so `BossBarragePocketCameraCueBridge` is generated and validated as enabled, matching both canonical scenes and the newly verified block/follow-up camera bridge. Focused policy report still passed after the correction with CombatPayload `PASS`, Axis 2 `PASS`, and block presentation cues `2/2/2`. This protects future scene regeneration without enabling the broad `ActionCameraCueDriver`.
- 2026-06-27 active hypothesis: Axis 2/3 still had a PGR-style major-punish presentation gap. Screen/camera/VFX bridges were verified, but the short `ActionCinematicCueDirector` frame-overlay path was still disabled in canonical setup, so `Block -> Status`, `NoHit -> Counter`, and `Skill1 Hit -> Presentation` did not all have a micro-cinematic read. The narrow scope is micro-cue director only; keep `ActionCinematicSequenceBridge` full playback disabled and do not add HUD/tutorial/proxy presentation.
- 2026-06-27 structural correction and batch evidence after follow-up micro-cinematic bridge: enabled `ActionCinematicCueDirector` cue playback and cinematic bars in the two canonical Frontline/BossBarrage scenes and generated setup, kept `allowSequenceBridgePlayback` false, added aggregate director counters to the policy report, and set the cue priority ladder to `BossPressureBreak 76 -> SummonRecall 77 -> SummonFollowupHit 78 -> PocketResult 80` under phase/boss/ultimate cut-ins. Focused policy runs plus repeat passed. Latest repeat records all structural gates `PASS`, physical barrage `0->4` hits, physical summon-punish `CleanFollowupClear`, bad routes reaching HP fail, and `Follow-up Cinematic Bridge` rows with director/sequence window-hit-miss: block/no-punish `1/0/1` and full punish/intended/recovery `1/1/0` or `1/1/1`, while sequence remains `0/0/0`. Focused authored-scene test `PocketClearsAfterCloseThreatDefeatedAndSummonBlocksBossFire` also passed after aligning result-copy expectations to the current stage profile (`PRESSURE BROKEN`, `Survive 3/3`). Boundary preserved: no new canonical scene, no full sequence playback, no broad VFX/audio restoration.
- 2026-06-27 prior-thread deep reread: added explicit failure-pattern guardrails after reviewing the linked thread's drift around new canon scenes, proxy/tutorial HUD, broad VFX/audio restoration, old-commit rollback, scene-assumption errors, and unrelated cleanup. The operational rule is now to keep this anchor as working memory: every implementation edit must name the missing ArkData/CombatPayload/PGR piece, its structural axis/metric, and the drift pattern it risks repeating.
- 2026-06-27 report guardrail extension after deep reread: added `Policy Repeatability Gate` to the batch report. The test now keeps the normal 21-policy report and then runs 9 representative policies 3 extra times each, writing 27 repeatability samples to markdown and JSON. Latest batch passed with all repeat verdicts `PASS`: no-summon and gun-only long routes repeatedly reached `PlayerDownFail`; backline physical barrage stayed `0` hits; forward-risk physical barrage stayed costly at `3-4` hits / `30-40` damage; physical summon block stayed `0` player hits with `2` blocks; no-punish stayed `Running` with counter pressure; physical summon-punish stayed `CleanFollowupClear` with `2` Skill1 hits, micro-cinematic hit cue `1`, and full sequence `0`; ignored boss-screen pressure stayed damaging while boss-screen recovery stayed `CounterRecoveryClear` with `0` HP lost. This closes the current measurement gap: the report now proves the structure is repeatable enough to guide the next ArkData-grounded feel decision instead of trusting one scripted run.
- 2026-06-27 report guardrail extension after repeatability: added `Stage/Wave Beat Map` to the policy report and JSON so detailed metrics collapse back into the NIKKE-like beat chain instead of becoming balance-only numbers. Latest batch passed with the expected classifications: `GunOnly` stalls at `ScreenCurtain`, physical summon block/no-punish stall at `FollowupConfirm`, boss-screen blocked/ignored stall at `CounterAnswer`, physical summon-punish and intended routes complete as `clean route complete`, and boss-screen recovery routes complete as `counter route complete`. This preserves the big premise after the repeatability pass: the next decision can see which stage beat is structurally missing before touching numbers, HUD, or presentation.
- 2026-06-27 canonical scene parity guardrail: bound the manual `ActionFoundationBossBarrageLaneReview.unity` pocket owner and HUD to `DB_FrontlineWaveStage_MotivationReview.asset`, updated setup/validation to preserve that binding, and added `BossBarrageLaneReviewScenePreservesFrontlineStageProfileParity`. The authored clear test now expects the profile's clean follow-up EN pulse (`205`, LV2) instead of the inline fallback (`125`, LV1), proving the manual scene is reading the same stage row as the policy loop. Focused PlayMode checks passed for the parity test and `PocketClearsAfterCloseThreatDefeatedAndSummonBlocksBossFire`; the broader class still has known unrelated drift failures such as missing `BossBarrageLaneReview_AmbientAudio`, and this pass did not restore broad audio/VFX to chase them.
- 2026-06-27 batch evidence after player-damage presentation bridge: added `Player Damage Presentation Bridge` to the policy report and JSON by exposing existing `ActionScreenCuePresenter` counters only. Two focused PlayMode report runs passed. Latest repeat records `NoSummonNoFire` HP lost `69.1`, player damage `15/0/0`, screen/feedback cues `15/15`; `GunOnly` HP lost `39.0`, cues `4/4`; `BossScreenIgnoredNoRecovery` cues `8/8`; clean physical summon-punish, intended clean routes, and boss-screen recovery stay `0/0`. Axis 3 and CombatPayload coverage now prove routine `Player Hit -> Presentation` without action-interrupting locks, while preserving the clean-route no-damage contrast and avoiding HUD/proxy or new presentation systems.
- 2026-06-27 batch evidence after counter-wave presentation bridge: added `Counter Wave Presentation Bridge` to the policy report and JSON by exposing existing counter-wave screen, camera, and VFX counters. Two focused PlayMode report runs passed. Latest repeat records physical no-punish as miss/counter `1/1` with counter presentation cues `1/1/1`, clean physical summon-punish as counter cues `0/0/0`, missed-followup recovery as wave/answer cues `1/1` screen, `1/1` camera, `1/1` VFX, and boss-screen recovery as `BossScreenBlock/ally_hold` with answer cues `1/1/1`. This closes `NoHit/Blocked Follow-up -> Counter Wave -> Presentation` without adding HUD, new cue systems, or broad presentation restoration.
- 2026-06-27 batch evidence after energy presentation bridge: added `Energy Presentation Bridge` to the policy report and JSON by exposing existing `ActionScreenCuePresenter` and `SummonEnergyVfxCuePresenter` counters. Two focused PlayMode report runs passed. Latest repeat records forward-risk energy as screen/VFX F/R/S `1/1/0` and `1/1/0`, physical clean punish as `3/3/2` and `1/3/2`, missed-followup recovery as `0/6/3` and `0/6/3`, and boss-screen recovery as ready/spend `6/4` screen and `6/4` VFX with counter answer pulse `105`. This closes `Counter Answer Pulse -> Energy Ready/Spend Presentation` without adding reward economy, stage select, tutorial HUD, or new presentation systems.
- 2026-06-27 active hypothesis: `Block-only Effect/Reward` is still too close to the player-authored Skill1 payoff. The report proves block-only/no-punish does not clear, but physical block-only still deals `133.8` summon boss damage while the player Skill1 payoff is `208.0`, and physical punish player share sits just above the gate at `69%`. ArkData/CombatPayload reason: the same pressure-slot answer should distinguish `answer started` from `result committed` not only by result state, but also by reward attribution. Narrow scope: lower ShieldBreaker assist/counter payoff only within the established role budget where ShieldBreaker volley remains below Marksman and above Vanguard; do not change scenes, HUD, reward economy, or summon roster.
- 2026-06-27 batch evidence after ShieldBreaker reward-floor split: kept ShieldBreaker as the screen/state opener and moved its assist/counter payoff close to, but still above, Vanguard while leaving Marksman as the damage slot. Final profile values are damage `48/63/83`, counter `15.36/20.16/26.56`, actor DPS `22/28/38`; targeted role-budget PlayMode test passed, so the slot still stays between Marksman damage and Vanguard defense. Two focused policy report runs passed. Latest repeat records physical block-only and no-punish still `Running` with boss damage player/summon `0.0/117.2`, 0 Skill1 hits, and no result records; physical summon-punish remains `CleanFollowupClear` with boss damage `208.0/78.7`, player share `72.5%`, 2 Skill1 hits, and repeatability `PASS`. This accepts the ArkData/CombatPayload split: `answer started` opens state and helps, but `result committed` is now more clearly the player-authored Skill1 confirmation. Drift avoided: no new scene, HUD/proxy, reward economy, broad presentation, or unrelated cleanup.
- 2026-06-27 batch evidence after Skill Gate Contract: added `PrematureSkill1NoSummon` to the policy report and repeatability gate after re-reading CombatPayload's `resource_cost_check -> target_selection -> projectile_or_hit_event -> presentation_feedback -> combat_log_or_state_commit` contract and PGR MagicId's buff/state-gated skill families. The new policy defeats the close probe, charges EN, fires Skill1 at the boss without a summon-opened follow-up state, then samples the remaining pressure. Two focused policy report runs passed. Latest repeat records premature Skill1 as `Running` with Skill1 use/hit `1/1`, player boss damage `84.0`, follow-up hit/cues `0/0/0/0`, result records `0`, repeatability `PASS`, and first unresolved stage beat `ScreenCurtain`. This closes a false-positive route gap: raw Skill1 damage can exist as chip, but only summon-opened state-gated Skill1 confirmation commits `CleanFollowupClear`. Drift avoided: no skill-system redesign, tutorial HUD, new scene, or broad presentation work.
- 2026-06-27 batch evidence after Target Priority Contract: added `BossTunnelVisionIgnoresCloseProbe` and a `Target Priority Contract` section to the policy report. ArkData reason: the NIKKE-like close/mid/far stage beat must prove close-probe priority before later pressure slots. CombatPayload reason: `target_selection` must distinguish boss chip from solving the current threat. Two focused PlayMode report runs passed. Latest repeat records boss tunnel vision as `Running` with close/boss basic hits `0/4`, close HP `72.0`, boss damage `56.0`, HP lost repeatability `40/40/40`, result records `0`, and first unresolved beat `CloseProbe`; `GunOnly` still proves the contrast as close/boss hits `6/5` and unresolved `ScreenCurtain`. Drift avoided: no new targeting system, no HUD/proxy instruction layer, no scene/profile changes, and no balance tuning.
- 2026-06-27 close-probe response guardrail: extended the `Target Priority Contract` and CombatPayload summary so close-probe local-defense hits now show `Target -> Effect/Status` in the same table as target priority. Two focused PlayMode report runs passed. Latest repeat records close target hits/damage/HP as `6/84.0/0.0` with response `6/0/0` non-lock/lock/full-body for `GunOnly`, `PrematureSkill1NoSummon`, and `IntendedRoute`, while boss tunnel vision stays `0/0.0/72.0` and response `0/0/0`. This keeps CloseProbe as a stage beat and local-defense target contract without enabling close-threat AI, changing balance, adding HUD/proxy guidance, or creating a new scene.
- 2026-06-27 active hypothesis: the Target Priority Contract still proves close-probe response through policy-forced contact/fire, but it does not yet prove the runtime selector sees the close-probe beat before the boss lane. ArkData reason: the close/mid/far stage row needs a pre-effect `Target` read before later pressure slots. CombatPayload reason: `target_selection` should be measurable before `Effect/Status`. PGR reason: the state should not depend on manual target selection or tutorial HUD. Next narrow attempt: add a policy-report selector probe that records the authored candidate count, default selector target, and local attack aim target, without adding a new targeting system, hard lock, HUD/proxy guidance, scene change, or balance tuning.
- 2026-06-27 batch evidence after local-defense selector probe: added `CloseProbeSelectorBiasProbe` to the policy report and repeatability gate. Two focused PlayMode report runs passed. Latest repeat records authored candidates `2`, selector default/attack aim `CloseProbe/CloseProbe`, close/boss distance `4.72/25.14`, aim angle close/boss `0.00/16.54`, and repeatability `3/3 PASS`. The top CombatPayload summary and Axis 1 gate now include this pre-effect target-selection proof before the existing boss-tunnel and close-response effect rows. Drift avoided: no target system rewrite, no hard-lock/direct selection UI, no HUD/proxy instruction layer, no scene/profile change, and no balance tuning.
- 2026-06-27 active hypothesis: close-probe target selection is now measured, but the policy report still uses forced contact/manual impact for the close-probe damage path. ArkData reason: the same close-probe stage beat should prove `Target -> Hit` through the live projectile path before later boss pressure. CombatPayload reason: `target_selection` should flow into `projectile_or_hit_event`, not only a scripted `TryApplyImpact`. PGR reason: the player should be able to answer the close state without manual target selection or tutorial HUD. Next narrow attempt: add a separate close-probe physical-fire probe that does not replace the stable policy route, does not add hard lock, and records actual projectile hits/damage against the close target while keeping boss damage at zero.
- 2026-06-27 active physical-fire evidence: the first `CloseProbePhysicalFireProbe` run deliberately failed, which is useful evidence rather than a committed milestone. The selector/aim read `CloseProbe/CloseProbe`, but 10 live `TryFire` shots produced `0` close hits, close HP stayed `72.0`, last impact came from `PF_BossSummonPressureActor_Proxy`, raw/resolved close angles were `27.5/29.9`, and boss pressure released at `4.7s` after shot 4. Missing piece: ArkData close slot must sit inside the first-beat camera/soft-fire pocket before the later pressure slot; CombatPayload `Target` must flow into an actual `projectile_or_hit_event`; PGR guardrail says do not solve this with hard lock, direct target UI, or tutorial HUD. Narrow attempt: move only the authored close-threat slot from lane x `-1.35` to `-1.05` in setup and both canonical scenes, keeping off-center no-bend tests as guardrails.
- 2026-06-27 physical-fire attempt result: lane x `-1.05` improved the live path from `0` to `1` close hit, but still failed the repeatability gate. Latest row: 10 shots, close projectile hits `1/1`, close HP `58.0`, boss damage `0.0`, selector default/aim `CloseProbe/CloseProbe`, fire aim `PF_BossSummonPressureActor_Proxy x1.00`, raw/resolved close angle `23.5/25.8`, boss pressure at `4.7s` after shot 5. Decision: this is still outside the stable first-beat soft-fire pocket. Next narrow attempt moves the authored close slot to lane x `-0.35`, preserving the no hard-lock/no tutorial HUD guardrail and keeping off-center no-bend tests as the regression boundary.
- 2026-06-27 guardrail correction during lane x `-0.35` validation: the policy report passed, but the old BossBarrage no-bend tests placed their "off-center" probe only `0.35/0.45` lateral units off the aim ray, which now falls inside the close body/direct viewport envelope and trips `HasAimAssistTarget` before the actual no-bend assertion. This was a stale test sample, not permission to add hard lock or hidden bend. Updated those guardrail samples to `1.35` lateral units so they again represent a true side-offset threat while the authored Frontline close slot remains inside the first-beat soft-fire pocket.
- 2026-06-27 batch evidence after close-slot physical-fire correction: lane x `-0.35` passed the focused policy report and a repeat run. Latest local-defense row records 6 shots, close physical hits `2/2`, close damage/HP `84.0/0.0`, close response `6/0/0`, boss hits/damage `0/0.0`, selector default/aim `CloseProbe/CloseProbe`, fire aim `BossProxy x0.46`, raw/resolved close angle `11.0/5.5`, and no boss pressure release before the close probe was answered. CombatPayload runtime pipeline and all four structural axes are `PASS`; repeatability records `CloseProbePhysicalFireProbe` as `PASS`. Guardrails also passed: Frontline near-center soft assist, BossBarrage peek-input no hidden assist, camera no-pull, and off-center no-bend with the corrected true side-offset sample. Drift avoided: no hard lock, target UI, tutorial HUD, new scene, or broad presentation pass.
- 2026-06-27 batch evidence after close-to-curtain stage-chain probe: added `CloseProbePhysicalThenScreenCurtainProbe` to prove the ArkData stage beat advances after the physical local-defense answer. Focused PlayMode report passed with repeatability `PASS`. Latest row: physical close HP `0.0`, boss releases `1`, max pressure screens `1`, result records `0`, first unresolved beat `ScreenCurtain`, stage judgement `stalled at ScreenCurtain`, and decision `summon_now(boss_curtain)`. CombatPayload summary now reports `close->curtain releases/screens 1/1`, so the chain is `CloseProbe Target->Hit` followed by a real `ScreenCurtain` pressure slot, not a HUD label or scripted test shortcut.
- 2026-06-27 active hypothesis: the stage chain is still split across probes. `CloseProbePhysicalThenScreenCurtainProbe` proves live `CloseProbe Target->Hit` advances to the summon-needed curtain, while `IntendedRoute` proves summon block -> Skill1 commit with a scripted close answer. Next narrow attempt: add one same-run policy that uses live close physical fire, then answers the resulting curtain with SummonSlot1 and state-gated Skill1, proving ArkData `CloseProbe -> ScreenCurtain -> FollowupConfirm -> Result` without a new scene, HUD/proxy target layer, balance-only tuning, or broad presentation work.
- 2026-06-27 batch evidence after live close-chain policy: added `CloseProbePhysicalThenSummonPunishProbe` and replaced its first helper with a natural screen-curtain wait instead of an extra scripted boss wave. Focused PlayMode report passed with repeatability `PASS` and no runner notes. Latest row: close physical hits `3/3`, close HP `0.0`, boss releases/screens `1/1`, summon blocks `2`, Skill1 hits `2`, follow-up/result `1/CleanFollowupClear`, result hook `clean_survival`, first unresolved beat `Complete`, and CombatPayload summary `live close-chain blocks/Skill1/result 2/2/CleanFollowupClear`. This closes the same-run ArkData chain from live `CloseProbe Target->Hit` into `ScreenCurtain Block -> Skill1 FollowupConfirm -> clean result` while preserving the guardrails: no new scene, no HUD/proxy target layer, no hard lock, no reward economy, and no broad presentation pass.
- 2026-06-27 active hypothesis: result hook structure is present, but the batch report still does not directly gate result copy/title/summary as wave/frontline survival language. ArkData reason: stage rows carry result/scenario hooks; the result should name the solved pressure, not only record reward-like text. CombatPayload reason: `state/log commit` should be inspectable after Hit/Presentation. PGR reason: completion records should tell the player what condition was satisfied. Next narrow attempt: add a result-copy contract to the policy report using existing `RouteResultRecord` fields, without adding payout/progression, stage select, reward economy, new HUD, or broad presentation work.
- 2026-06-27 batch evidence after result-copy contract: policy report now gates `RouteResultRecord` title/summary/route label in the Stage Result Hook Contract. Focused PlayMode report passed. Latest stage-result summary records bad routes with copy `PLAYER DOWN`/`PLAYER DOWN`, clean physical and live close-chain as `PRESSURE BROKEN: Summon opening confirmed; Skill1 follow-up landed [Clean summon follow-up]`, and boss-screen recovery as `PRESSURE BROKEN: Counter pressure held; final follow-up confirmed [Counter recovery]`. All committed hooks remain `review-only analysis`, and the copy contract rejects generic boss-HP result language plus payout/progression language.
- 2026-06-27 active hypothesis: the Stage Result Hook Contract is proven in the policy loop, but the manual `ActionFoundationBossBarrageLaneReview` fail test still expected stale generic copy (`MISSION FAILED`). ArkData reason: the authored stage/result hook should name the failed pressure condition, not a generic mission outcome. CombatPayload reason: `state/log commit` must remain inspectable through the same result banner in the manual scene. Narrow scope: align the manual fail-copy assertion to the current profile-driven `PLAYER DOWN` / HP-pressure summary only; do not add reward economy, tutorial HUD, proxy guidance, or new presentation systems.
- 2026-06-27 focused authored-scene evidence after manual fail-copy alignment: `PocketFailureStopsEnergyGainAndBossPressure` now passes with the manual BossBarrage HUD showing `PLAYER DOWN`, `Player HP reached zero`, survival/time suffix copy, and explicit rejection of stale `MISSION FAILED`/`BOSS CLEAR` text. `FrontlinePlayerDefeatKeepsPlayerDownFailureReason` also passes, confirming the manual scene and Motivation review scene share the same profile-driven player-down result hook. Drift avoided: no runtime balance change, no reward/progression hook, no tutorial/proxy HUD, and no new presentation path.
- 2026-06-27 active hypothesis: the policy report proves `ForwardRiskPhysicalSummonBlockProbe` and `ForwardRiskPhysicalSummonPunishProbe`, but the manual `ActionFoundationBossBarrageLaneReview` clear test still uses direct `TryIntercept` for the boss projectile. ArkData reason: the same authored LinePressure wave should prove both the unblocked `Hit` cost and the summon `Block` answer in the manual scene. CombatPayload reason: `Target geometry -> projectile flight -> Block/Status -> Skill1 Hit -> state/log commit` should close without a helper-forced intercept. Narrow scope: add one manual scene PlayMode contract that uses real flying LinePressure projectiles and existing summon/Skill1 systems; do not add a new scene, hard lock, HUD/proxy guidance, balance tuning, or presentation system.
- 2026-06-27 active finding during manual physical-path validation: authored LinePressure projectiles spawn at the boss proxy lane point and immediately overlap non-route colliders around the boss-side source before they can travel through the summon screen. Narrow attempt: clear only `BossBarrageEmitter` projectile spawn points out of the source collider volume so the manual scene can prove CombatPayload `Target geometry -> projectile flight -> Block`, without changing HUD, scene ownership, reward economy, target UI, or balance tables.
- 2026-06-27 focused authored-scene evidence after physical LinePressure path fix: `PocketPhysicalLinePressureSummonBlockClearsThroughRealProjectilePath` now passes using the real authored `LinePressure` volley, an active `SummonPressureScreen`, and real Skill1 projectile confirmation. The fix was structural, not balance: LinePressure speed stayed at the original 13, the manual test waits for the close-probe relief beat before firing the curtain, and the projectile path now starts outside the source body and polls active pressure screens during flight.
- 2026-06-27 batch evidence after policy probe alignment: `WritesFrontlineCombatPolicyReport` passes again after isolating the physical LinePressure probes from the separate forward-risk EN charging probe and filtering tracked boss projectiles by the active pattern material. Latest repeatability gate records `ForwardRiskPhysicalSummonBlockProbe` as `PASS` with HP lost 0, blocks 2, physical player hits 0/4, and block->window 0.0s; `ForwardRiskPhysicalSummonPunishProbe` as `PASS` with HP lost 0, blocks 2, Skill1 hits 2, `CleanFollowupClear`, boss damage 286.7, and player/summon attribution 208.0/78.7. This preserves the ArkData beat split: energy-risk, physical curtain block, no-punish counter, and committed Skill1 result remain separate measured rows.
- 2026-06-27 active hypothesis: the policy report proves `BossScreenBlockCounterRecovery`, but the manual `ActionFoundationBossBarrageLaneReview` scene only verifies the boss-screen block as a follow-up failure. ArkData reason: the same stage row should prove a blocked follow-up can reopen into a counter-result branch, not stop at a state label. CombatPayload reason: `NoHit/Blocked -> Status lock -> fresh summon answer -> Skill1 Hit -> state/log commit` should be inspectable in the authored scene. PGR reason: a QTE-like failed punish should immediately expose the recovery answer and final punish window. Narrow scope: add one manual scene contract that extends the existing boss-screen block flow through fresh `SummonSlot1`, final Skill1, and `CounterRecoveryClear`; do not add a new scene, HUD/proxy guidance, reward economy, balance-only tuning, or broad presentation work.
- 2026-06-27 focused authored-scene evidence after boss-screen recovery contract: added `PocketBossScreenBlockCounterRecoveryClearsWithFreshSummonAnswer`, proving the manual boss-barrage scene now runs boss-screen block -> counter answer pulse -> fresh `SummonSlot1` -> ally hold -> final Skill1 hit -> `CounterRecoveryClear` result copy. The older blocked-follow-up branch still passes after aligning its objective-copy assertion to the current profile text (`Boss screen absorbed the follow-up; rebuild the summon answer`). Focused PlayMode checks passed twice for the new recovery test and once for the blocked-only branch.
- 2026-06-27 batch evidence after boss-screen manual recovery contract: `WritesFrontlineCombatPolicyReport` passes again. Latest repeatability records `BossScreenBlockCounterRecovery` as `PASS` across 3 runs with `CounterRecoveryClear`, HP lost `0/0/0`, boss damage `208/208/208`, blocks `2/2`, Skill1 `2/2`, and counter presentation `1/1/1`; stage result copy remains `PRESSURE BROKEN: Counter pressure held; final follow-up confirmed [Counter recovery]`. During the rerun, the block-only physical probe exposed an over-specific repeatability threshold (`SummonBlocks >= 2`) even though the structural contract is no player hit/damage, at least one real block, and immediate follow-up window; the gate now matches the existing top-level structural assertions (`SummonBlocks > 0`) without changing runtime balance or presentation.
- 2026-06-27 active hypothesis: the policy report proves forward-risk LV1 speed and physical forward-risk danger, but it still does not directly measure `EN LV1 -> LV2 -> LV3` as a decision ladder. ArkData reason: stage pressure should expose whether waiting through more pressure slots is a readable risk/reward choice. CombatPayload reason: Resource Effect should be measured beyond the first readiness state, including HP/down cost while waiting. PGR reason: tier/buff-gated power should not collapse into "always wait for LV3" or "never wait past LV1." Next narrow attempt: add backline-vs-forward EN tier ladder probes that record LV1/LV2/LV3 ready times, HP cost, player-down timing, and band dwell before any runtime tuning.
- 2026-06-27 batch evidence after EN tier-decision reporting: added `BacklineEnergyTierLadderProbe` and `ForwardRiskEnergyTierLadderProbe` plus an `EN Tier Decision Pressure` report section and JSON fields for LV2/LV3 ready times and HP/s. `dotnet build` passed and two focused PlayMode report runs passed (`EnergyTierDecision-2`, `EnergyTierDecision-3`). Latest repeat records backline LV3 at `30.6s` / `56.4` HP / `1.8` HP/s, and forward-risk LV3 at `11.3s` / `54.8` HP / `4.8` HP/s. Interpretation: forward-risk waiting is much faster and compresses pressure into a higher damage rate, but total HP cost is similar to backline. This is a structural measurement milestone, not a balance change; next decision should judge whether "fast LV3 with high HP/s" is enough choice tension or whether the stage needs a stronger answer-now versus wait-longer split.
- 2026-06-27 batch evidence after EN spend-decision route reporting: added `ForwardRiskTier1DecisionRoute`, `ForwardRiskTier2DecisionRoute`, and `ForwardRiskTier3DecisionRoute` to spend SummonSlot1 after waiting under forward-risk pressure, then run the same physical `LinePressure` block -> Skill1 branch. Focused PlayMode report `EnergySpendDecision-8` passed. Latest report records LV1/LV2/LV3 ready at `3.8s` / `7.6s` / `11.3s`, HP lost `40.0` / `50.8` / `70.9`, summon spend tiers `1` / `2` / `3`, follow-up windows `2.3s` / `2.7s` / `3.1s`, and pulse `205` / `205` / `240`; all three correctly block the physical barrage with summon but end `Running` because boss-screen pressure blocks Skill1 and leaves `CounterAnswer` unresolved. Interpretation: higher EN spend creates larger window/pulse, but it is not yet a clean payoff loop. The next structural decision is whether the waited-tier route should intentionally become a recovery branch, or whether higher spend should introduce a justified boss-screen suppress/pierce/stagger state.
- 2026-06-27 user reminder preserved without derailing current branch: the three summon roster members' individual mana costs and effects currently risk reading as interchangeable. This belongs to a later summon-roster identity pass, not the current EN spend branch unless the data points there. When that pass starts, compare each summon as a separate cost/effect role: low-cost emergency answer, mid-cost stable counter/line hold, high-cost board-turning answer. Guardrail: do not solve this first through final UI/resource polish; define the state/effect difference and prove it in batch metrics before Blue Archive-like ready feedback, coaster fill, final icons, audio, or authored UI resources are treated as motivation.
- 2026-06-27 batch evidence after EN spend recovery-branch reporting: added `ForwardRiskTier1RecoveryRoute`, `ForwardRiskTier2RecoveryRoute`, and `ForwardRiskTier3RecoveryRoute` to continue the blocked EN spend branch through fresh counter summon answer -> final Skill1 -> `CounterRecoveryClear`. Also added a shared active-ally-screen wait before physical barrage probes, after one repeatability run exposed a real measurement race where `ForwardRiskPhysicalSummonPunishProbe` could fire before the summon screen was active and record `0` blocks. `dotnet build` passed; PlayMode `EnergySpendRecovery-3` and `EnergySpendRecovery-4` both passed after the timing guard. Latest report records direct LV1/LV2/LV3 spend as still `Running` at `CounterAnswer`, while recovery LV1/LV2/LV3 clears with ready times `3.8s` / `7.6s` / `11.3s`, HP lost `30.0` / `50.8` / `70.9`, counter->answer `2.8s` / `2.8s` / `3.2s`, answer->stable `0.4s`, final->hit `0.1s`, and player boss damage `208.0` for all tiers. Interpretation: the branch can close as ArkData/PGR-style recovery, but high-tier spend still does not create a distinct direct payoff; it only pays more risk before the same boss-screen block and a second summon answer. Next structural choice should compare this recovery-branch cost against a justified high-tier boss-screen suppress/pierce/stagger state, not tune numbers blindly.
- 2026-06-27 batch evidence after summon roster identity audit: added `Summon Roster Mana/Effect Identity Audit` to the policy report and JSON so the user reminder about summon mana/effect identity stays inside the ArkData/CombatPayload/PGR premise instead of derailing into UI polish. `dotnet build` passed, and focused PlayMode `RosterAudit-4` passed after correcting the runner to wait through the real physical barrage flight window for follow-up confirmation. Latest audit records cost verdict `CHECK` because all three slots still read the shared `SummonEnergyLadder` as `100/100/100`, with no slot-specific summon mana yet. Effect verdict is `PASS`: `SummonSlot1.ShieldBreaker` is the screen/counter opener, `SummonSlot2.BacklineMarksman` is the high-volley no-screen damage slot, and `SummonSlot3.VanguardCommander` is the high-HP screen holder. Interpretation: unit effect roles have data contrast, but summon mana identity is still missing; next roster work should introduce or reject a per-slot cost/resource contract by simulation before Blue Archive-like ready coaster feedback, final icons, audio, or authored UI resources are treated as motivation.
- 2026-06-27 batch evidence after slot-specific summon mana gates: added a narrow `minimumSummonTier` contract to `PlayerSupportSummonSlotAction` instead of creating a roster manager or new economy. Canonical scenes now read Slot1 as the LV1 emergency answer, Slot2 as the LV2 marksman gate, and Slot3 as the LV3 vanguard gate. Focused policy report `SlotManaGate-2` passed and now records cost verdict `PASS`: required summon mana is `100/200/300` while the shared ladder tier targets remain `100/100/100`. Effect verdict remains `PASS`, so the cost split did not erase the role split. Focused Duel PlayMode `SlotManaGate-4` also passed after proving Slot2 is blocked at LV1 (`Requires LV2 EN`) and usable at LV2. During verification, a repeatability gate was corrected to judge the low-tier decision route by its structural state (`Running` at `CounterAnswer`) rather than a flaky blocked-followup bool; this preserves the intended ArkData/PGR read that low-tier spend starts recovery work while high-tier spend can directly suppress. Boundary: this is the resource contract behind future ready feedback, not the coaster UI, icon/audio pass, reward economy, or full summon roster.
- 2026-06-27 correction after user clarification: the cost identity contract is now explicit per summon, not only inferred from `minimumSummonTier`. `PlayerSummonSlot1Action` and `PlayerSupportSummonSlotAction` carry `requiredSummonMana`; canonical scenes author Slot1/Slot2/Slot3 as `100/200/300`; the roster audit reads those action values directly while still showing the shared ladder tier targets as `100/100/100`. `dotnet build` passed, focused policy report `ExplicitSummonMana-1` passed, and focused Duel PlayMode `ExplicitSummonMana-1` passed. Latest audit records cost verdict `PASS explicit per-summon mana costs split 100/200/300` and effect verdict `PASS`, preserving the bigger premise: this is data/state identity for later route simulation and ready feedback, not a detour into UI polish or final economy.
- 2026-06-27 batch evidence after support route identity reporting: added target-confirmed support projectile hit metrics and `ForwardRiskSlot2MarksmanRoute` / `ForwardRiskSlot3VanguardRoute` policy rows. Focused policy report `SupportProjectileTargets-3` passed. Latest repeatability records Slot2 as `PASS` with mana `200`, tier `2`, projectile hits `6/6`, blocks `0/0`, physical player hits `4/4`, and boss summon damage `0`; the route read is `marksman suppresses enemy frontline but leaves physical pressure`. Slot3 records mana `300`, tier `3`, projectile hits `4/4`, blocks `7/7`, physical player hits `0/0`, and boss summon damage up to `57.3`; the route read is `vanguard spends more mana to hold the barrage line`. Interpretation: the roster is no longer only cost-separated; Slot2 is a lower-cost no-screen enemy-frontline suppressor, while Slot3 is the higher-cost physical-line hold. Boundary: this is route/effect measurement, not final summon balance, ready UI, icon/audio feedback, reward economy, or roster expansion.
- 2026-06-27 Blue Archive EX resource check: searched ArkData `SubcultureGameData/games/blue-archive` SchaleDB snapshot. `students.json` confirms 194 EX rows with cost distributions centered on `3/4/5` and spread from `2` to `10`; the EX object fields are `SkillType`, `Name`, `Desc`, `Parameters`, `Cost`, `Duration`, `Range`, `Radius`, `Icon`, and `Effects`, with no `Cooldown`/`CoolTime`. Decision: do not invent separate summon gauges. Introduce only the missing contract the user clarified: one shared summon mana bank, each summon slot spends its own required mana (`100/200/300`) and starts only its own cooldown; other slots remain selectable if the shared bank still covers their cost. Drift avoided: no final coaster UI, icon/audio polish, reward economy, or broad roster expansion before the resource-state contract is proven in PlayMode.
- 2026-06-27 active correction after shared-mana cooldown regression: per-slot cooldown is valid for normal summon choice, but a counter-wave answer pulse is also a PGR-like answer unlock. If Slot1 remains on cooldown after the boss-screen block, `BossScreenBlockCounterRecovery` loses the fresh-answer route even though the shared mana contract is correct. Narrow attempt: when `GrantCounterWaveAnswerEnergyPulse` fires, clear only Slot1 cooldown so the answer window opens; do not clear Slot2/Slot3 cooldowns, remove the independent cooldown model, add UI, or hide the result with balance tuning.
- 2026-06-27 batch evidence after shared summon mana and counter-answer unlock: `dotnet build` passed, `EnergyLadderCanSpendSummonCostWithoutResettingSharedManaBank` passed with `EnergyChanged`/`EnergySpent` on partial mana spend, `DuelReviewSummonSlotsSpendSharedManaWithoutStartingGlobalCooldown` passed, and focused policy report `SharedManaSlots-4` passed. Latest report keeps all four structural gates `PASS`, roster mana/effect audit `PASS` with costs `100/200/300`, and `BossScreenBlockCounterRecovery` repeatability `PASS` across 3 runs as `CounterRecoveryClear` with HP lost `0/0/0`, boss damage `208/208/208`, blocks `2/2`, Skill1 `2/2`, and counter cues `1/1/1`. Interpretation: Blue Archive-like shared resource choice is now represented as resource-state grammar without losing the ArkData/PGR counter-recovery loop.
- 2026-06-27 active hypothesis after shared resource contract: Slot2/Slot3 now have cost/effect identity, but the stage beat map still reads them like measurement probes instead of partial roster answers. ArkData reason: roster rows should state which pressure slot they answer and which next beat remains. CombatPayload reason: `SupportSummon Target -> Effect` must become a visible `Status` row, not only projectile/block counts. PGR reason: choosing the wrong EX-like answer should be legible as a partial answer, not a silent failed route. Narrow attempt: add `SupportAnswer` to the policy report beat map/JSON and assert Slot2 suppresses enemy frontline while leaving `ScreenCurtain`, and Slot3 holds the physical line while still leaving `ScreenCurtain`; do not turn either into a forced clear, add UI, or change combat balance yet.
- 2026-06-27 batch evidence after support-answer beat map: `dotnet build` passed and focused policy report `SupportBeat-1` passed. The Stage/Wave Beat Map and JSON now classify `ForwardRiskSlot2MarksmanRoute` as `support=SUPPRESS_ENEMY_FRONT` with first unresolved `ScreenCurtain`, after 6 enemy-frontline projectile hits and 4/4 physical player hits. `ForwardRiskSlot3VanguardRoute` classifies as `support=HOLD_PHYSICAL_LINE` with first unresolved `ScreenCurtain`, after 7 support blocks and 0/4 physical player hits. All four structural gates remain `PASS`. Interpretation: Slot2 and Slot3 are now visible as partial EX-like roster answers; the next gameplay decision can ask which follow-up state should close the remaining `ScreenCurtain`, instead of mistaking their current `Running` result for generic failure or tuning damage blindly.
- 2026-06-27 active hypothesis after support partial-answer proof: the shared-mana model should create combo choice, not just three isolated buttons. ArkData/Blue Archive reason: EX-like cost identity matters because a lower-cost skill can leave resource for a second answer, while a high-cost skill can consume the whole turn. CombatPayload reason: `SupportSummon Effect` should be followed by an inspectable `Resource -> MainAnswer` state if mana remains. PGR reason: route choice should expose a lock/unlock consequence, not only damage/hold numbers. Narrow attempt: add policy rows for `Slot2 -> Slot1` and `Slot3 -> immediate Slot1` from a full 300 mana bank. Expected result: Slot2 leaves 100 mana and can close the remaining `ScreenCurtain` with Slot1 + Skill1, while Slot3 spends 300, holds physical pressure, but cannot immediately pay Slot1; do not change runtime balance, UI, or force Slot3 into a clear.
- 2026-06-27 batch evidence after shared-mana support combo reporting: added `ForwardRiskSlot2ThenSlot1ComboRoute`, `ForwardRiskSlot3ThenSlot1BlockedRoute`, and the `Shared Mana Support Combo Branch` report/JSON fields. First batch attempt exposed a useful miss: immediate Slot2 -> Slot1 preserved the 100 EN Slot1 answer but fired before marksman suppression had enough time, ending `Running` at `FollowupConfirm` with 0 Skill1 hits. The policy route was corrected to wait only for Slot2's support-fire timing beat before Slot1. Focused PlayMode `SharedManaCombo-3` passed. Latest repeatability records Slot2 as `CleanFollowupClear` across 3 runs with EN after support `100`, EN before Slot1 `~117.3`, Slot1 used, 2 summon blocks, 5 enemy-frontline support hits, 2 Skill1 hits, and boss damage `238.7`; Slot3 records EN after support/before Slot1 `0/0`, immediate Slot1 blocked as `Requires 100 EN`, 7 blocks, 0 physical player hits, 0 Skill1 hits, and first unresolved `ScreenCurtain`. Interpretation: the Blue Archive-like shared-resource choice is now measured as combat structure: lower-cost Slot2 can preserve resource for a second answer after its effect beat, while high-cost Slot3 buys physical safety but consumes the immediate main-answer turn. Boundary: no runtime balance, UI/coaster feedback, reward economy, scene switch, or roster expansion was introduced.
- 2026-06-27 active hypothesis after full-bank combo proof: the next missing piece is not UI. The report still proves Slot2 -> Slot1 from a full 300 EN bank, while real play should also measure the earlier EX-like spend: use Slot2 at its LV2/200 EN timing, let its support effect happen, then survive/recharge into Slot1. ArkData reason: low-cost EX choices often buy tempo before the next card, not only preserve leftover resource at max bank. CombatPayload reason: `Resource Spend -> Support Effect -> Recharge/Exposure -> MainAnswer` should be inspectable as distinct states. PGR reason: partial answers should expose a delayed unlock path, not only immediate lockout. Narrow attempt: add delayed Slot1 policy rows after Slot2-at-200 and Slot3-at-300 support spends, recording Slot1-ready delay, HP cost before Slot1, support hits/blocks, and final result; do not add UI/coaster feedback, change runtime balance, or treat visual opacity/material drift as part of this evidence.
- 2026-06-27 batch evidence after delayed support main-answer reporting: added `ForwardRiskSlot2ThenDelayedSlot1Route` and `ForwardRiskSlot3ThenDelayedSlot1Route` to the policy report plus markdown/JSON fields for Slot1-ready delay and HP cost before Slot1. `dotnet build` passed; PlayMode `DelayedCombo-3` passed after two useful rejected expectations. Latest report records Slot2-at-LV2 as EN after support `0.9`, Slot1 ready after `3.1s`, HP lost before Slot1 `50.8`, 6 enemy-frontline support hits, 0/4 physical player hits, Slot1 used, but 0 Skill1 hits and `CounterAnswer` / `Running`. Slot3-at-LV3 records EN after support `0`, Slot1 ready after `3.8s`, HP lost before Slot1 `70.9`, 7 support blocks, 0/4 physical player hits, Slot1 used, 1 Skill1 hit, and `CounterAnswer` / `Running` in the main report; repeatability showed Slot3 as mixed `Running` and `CleanFollowupClear` with direct clears in 2/3 samples. Interpretation: early support spend does reopen the main answer, but Slot2 is a stable counter-recovery branch and Slot3 is a high-cost hold that can sometimes beat the boss-screen timing but is not yet a stable direct clear. Next narrow attempt should add delayed-support counter-recovery policies and compare time/HP/result, not tune UI, opacity/materials, reward economy, or broad combat balance.
- 2026-06-28 batch evidence after delayed support counter-recovery verification: latest code includes `ForwardRiskSlot2ThenDelayedRecoveryRoute` and `ForwardRiskSlot3ThenDelayedRecoveryRoute`. After the separate cutscene commits, `dotnet build` passed and PlayMode `DelayedRecovery-Verify2` passed. Latest report records Slot2 delayed recovery as Slot1 ready after `3.1s`, HP lost before Slot1 `50.8`, physical hits `0/4`, `CounterRecoveryClear`, counter->answer `2.9s`, answer->stable `0.4s`, final->hit `0.1s`, Skill1 hits `2`, result records `1`. Slot3 delayed recovery records Slot1 ready after `3.8s`, HP lost before Slot1 `70.9`, physical hits `0/4`, `CounterRecoveryClear`, counter->answer `2.7s`, answer->stable `0.4s`, final->hit `0.1s`, Skill1 hits `3`, result records `1`. Interpretation: both early support spends now reopen the main answer and close through an ArkData/PGR-style recovery branch; Slot3 can sometimes direct-clear in the main-answer sample, but the recovery policy proves the stable close. The `counter->answer` delay is measured within the physical barrage observation window, not an immediate QTE cue. Next narrow attempt: add a compact support decision matrix summary so Slot1 immediate, Slot2 full-bank combo, Slot2 delayed recovery, Slot3 immediate lockout, and Slot3 delayed recovery can be judged side by side by cost, HP exposure, physical hits, recovery burden, and result before any UI/coaster feedback or balance tuning.
- 2026-06-28 batch evidence after support decision matrix reporting: added `Support Decision Matrix` near the top of the policy report. `dotnet build` passed and PlayMode `SupportDecisionMatrix-1` passed. Latest matrix reads: Slot1 LV1 recovery `100` cost, HP before main `40.0`, physical hits `0/4`, recovery burden `counter 2.8s -> hit 0.1s`, `CounterRecoveryClear`; Slot2 full-bank combo `200 -> 100`, HP `70.9`, physical hits `0/4`, no recovery burden, `CleanFollowupClear`; Slot2 delayed recovery `200 -> recharge -> 100`, HP `50.8`, physical hits `0/4`, recovery burden `counter 2.8s -> hit 0.1s`, `CounterRecoveryClear`; Slot3 immediate lockout `300 -> 0`, HP `70.9`, physical hits `0/4`, `Running/ScreenCurtain`; Slot3 delayed recovery `300 -> recharge -> 100`, HP `70.9`, physical hits `0/4`, recovery burden `counter 2.7s -> hit 0.1s`, `CounterRecoveryClear`. Interpretation: the roster now has real route-choice separation in the report, but Slot2 full-bank still asks for high HP exposure to earn a no-recovery clear and Slot3 immediate is intentionally a lockout. Next narrow attempt should decide whether these tradeoffs are satisfying enough or whether one route needs a clearer stage-state payoff; do not jump to final UI/coaster feedback until the route-choice read is accepted.
- 2026-06-28 batch evidence after support decision dominance read: extended the matrix with a dominance line and verified PlayMode `SupportDecisionMatrix-2`. Latest read says Slot2 delayed recovery matches Slot1's recovery result but costs more and shifts HP by `+10.8`; Slot2 full-bank pays `+30.9` HP for no recovery burden and `CleanFollowupClear`; Slot3 immediate stays `Running/ScreenCurtain` despite line hold; Slot3 delayed recovery pays `+30.9` HP for stable `CounterRecoveryClear`. Interpretation: the route-choice data is real but not fully satisfying yet. Slot2/Slot3 have cost/effect identity, but the delayed support paths risk feeling dominated by the cheaper Slot1 recovery unless their payoff becomes clearer. Next narrow attempt should be a structural payoff comparison/prototype for one support route, not UI, final icons, reward economy, or broad balance tuning.
- 2026-06-28 batch evidence after vanguard assist payoff prototype: introduced a narrow Slot3-only bridge from `HOLD_PHYSICAL_LINE` to the next Skill1 boss-screen suppression. ArkData reason: the high-cost vanguard support needed a stage-state payoff, not another generic recovery row. CombatPayload reason: `SupportSummon Target -> Effect` now leaves a temporary `Status` that the follow-up `Effect` can consume. PGR reason: a high-cost guard/parry-like answer can unlock a direct punish instead of relocking into a second answer. `dotnet build` passed, focused PlayMode `VanguardAssist-SceneRefs-3` passed with scene-serialized Slot2/Slot3 assertions, and `ValidateFrontlineMotivationReviewSceneMenu` passed. Latest report records Slot2 delayed recovery unchanged as `CounterRecoveryClear`, HP before Slot1 `50.8`, boss suppress `0/0`, boss-screen block true, counter wave `1`, Skill1 hits `2`; Slot3 delayed recovery now records `CleanFollowupClear`, HP before Slot1 `70.9`, boss suppress `2/3`, boss-screen block false, counter waves `0`, Skill1 hits `1`. Interpretation: Slot3 is no longer only the expensive version of Slot1 recovery; it pays extra HP/time for line safety and a direct boss-screen suppress confirm. Boundary: this is a measured state/effect payoff, not final UI/coaster feedback, reward economy, broad VFX/audio, or a new scene.
- 2026-06-28 timing correction after support decision verdicts: added a `Timing verdict` column/assertion to the support decision matrix and fixed a runner blind spot where a lingering Slot3 vanguard screen could be mistaken for the later Slot1 pressure screen. Support combo probes now wait for a new Slot1 pressure screen and require a new follow-up window after the physical barrage before confirming Skill1. `dotnet build` passed and focused PlayMode `SupportTimingVerdict-3` passed. Latest repeatability records Slot2 delayed as `PASS` with 3/3 `Running` at `CounterAnswer`, HP before Slot1 `49.4/49.9/50.8`, boss damage `17.9/17.9/17.9`, Skill1 `0/0`; Slot3 delayed as `PASS` with 3/3 `CleanFollowupClear`, HP before Slot1 `70.9/70.9/70.9`, boss damage `199.7/222.3/267.4`, blocks `9/9`, support projectiles `4/4`, Skill1 `1/2`. Interpretation: Slot2 LV2 delayed is intentionally a mistimed marksman spend that still needs counter recovery, while Slot3 delayed is the intended vanguard payoff; future work should preserve that route-choice read instead of buffing every delayed support path into the same result.
- 2026-06-28 batch evidence after support-specific result hooks: split the existing review-only clear result hook for support-assisted clean routes without adding reward economy, UI/coaster feedback, or new presentation assets. ArkData reason: stage/result rows should tell the player which route cleared, not collapse all clean routes into one generic copy. CombatPayload reason: the measured `SupportSummon Effect -> Skill1 Hit` path now reaches a distinct Presentation/result record. `dotnet build` passed and focused PlayMode `SupportResultHook-1` passed. Latest result hook contract records Slot2 full-bank combo as `support_marksman_clear` with copy `PRESSURE BROKEN: Marksman support suppressed the frontline; Slot1 preserved the Skill1 confirm [Clean LV3 marksman follow-up]`, and Slot3 delayed payoff as `support_vanguard_clear` with copy `PRESSURE BROKEN: Vanguard hold carried into boss-screen suppression; Skill1 follow-up landed [Clean LV3 vanguard follow-up]`. Repeatability stayed `PASS`: Slot2 full-bank 3/3 `CleanFollowupClear`, boss damage `238.7/238.7/238.7`; Slot3 delayed 3/3 `CleanFollowupClear`, boss damage `199.7/244.8/267.4`. Interpretation: support route choice now has result-level motivation feedback tied to the same batch-proven structure, while final UI, icons, audio, rewards, and coaster readiness remain later work.
- 2026-06-28 batch evidence after enemy pressure tactical-cost matrix: promoted existing Axis 4 metrics into a top-level `Enemy Pressure Tactical Cost Matrix` and added assertions that unattended/missed/ignored branches create body/HP cost while recovered/clean branches redirect or prevent that cost. ArkData reason: enemy pressure actors should read as stage pressure slots with target/status changes, not timer-only bookkeeping. CombatPayload reason: the same pressure actor now reports `Target` shift from player body to summon clash before result resolution. `dotnet build` passed and focused PlayMode `EnemyTacticalCost-1` passed. Latest matrix records no-action `9` body hits / `69.1` damage / `70%` unanswered burden, block-without-punish `15` body hits / `20.2` damage / `83%`, ignored boss-screen counter `8` body hits and `10` summon hits / `10.8` damage / `71%`, counter recovery `0` body hits and `1` summon hit / `0.0` damage / `CounterRecoveryClear`, and clean physical punish `0` body hits / `0.0` damage / `CleanFollowupClear`. Interpretation: Axis 4 now shows tactical cost shape at the decision layer: ignoring or missing the answer is costly but not instant death, while summon/recovery structure changes the pressure target and clears.
- 2026-06-28 batch evidence after combat decision-signal matrix: added a top-level `Combat Decision Signal Matrix` and assertions tying each player decision beat to measured cue/readout evidence before any UI/coaster or balance pass. `dotnet build` passed and focused PlayMode `DecisionSignal-1` passed. Latest matrix records risk-for-EN as ForwardRisk band `3.9s`, LV1 ready `3.8s`, HP `40.0`, EN screen/VFX `1/1/0`; ignored pressure as `9` body hits, HP `40.1`, player damage cues `15/15`; summon block as `2` blocks, window `0.0s`, block cam/flash/VFX `4/2/2`, window screen/cam/VFX `1/1/1`; skipped Skill1 as miss/counter `1/1`, body hits `15`, miss and counter cues `1/1/1`; counter answer as pulse `105`, stable `0.5s`, answer cues `1/1/1`, EN ready/spend `6/4`; clean Skill1 confirm as boss damage `208.0`, HP leak `0.0`, hit cues `1/1/1`, cine/frame `1/1`. Interpretation: the current loop now has a decision-layer signal audit; next gameplay changes should use this table to identify missing decision readability or payoff before touching final UI, broad presentation, reward economy, or blind damage tuning.
- 2026-06-28 decision-state correction after signal audit: kept the existing stage-wave `FirstUnresolvedBeat` semantics intact, but changed `Combat Decision Signal Matrix` to report a decision-layer state so it no longer hides relock consequences behind the earlier failed beat. `dotnet build` passed and focused PlayMode `DecisionState-1` passed. Latest matrix now reads `Running/ENReady`, `Running/BodyPressure`, `Running/FollowupReady`, `Running/CounterAnswer`, `CounterRecoveryClear/CounterRecovered`, and `CleanFollowupClear/Complete`. Interpretation: stage analysis can still say no-punish first failed at `FollowupConfirm`, while the player-decision layer correctly says the next required answer is `CounterAnswer`; this preserves the ArkData/PGR lock-unlock premise without tuning damage or adding UI.
- 2026-06-28 batch evidence after stage-result motivation matrix: promoted result-hook motivation into a top-level `Stage Result Motivation Matrix` without adding payout, progression, campaign clear, or reward economy. ArkData/NIKKE reason: stage outcomes need distinct result/next-run hooks, not only pass/fail rows. `dotnet build` passed and focused PlayMode `ResultMotivation-1` passed. Latest matrix records `failure_analysis` for no-summon and gun-only failures with "protect HP" motivation, `clean_survival` for the clean summon confirm with "confirm before counter pressure", `counter_recovery` with "answer counter earlier", `support_marksman_clear` with "spend Slot2 from a full bank", and `support_vanguard_clear` with "commit Slot3 when line safety is worth the delayed main answer"; every row remains `review-only analysis`. Interpretation: the combat loop now has a measured result-motivation layer that can support later reward/UI work, while still avoiding reward economy and broad presentation drift.
- 2026-06-28 batch evidence after decision-signal repeat bands: extended `Combat Decision Signal Matrix` with a `Repeat signal` column so top-level decision reads are judged from repeat samples, not a single pretty or unlucky run. `dotnet build` passed and focused PlayMode `DecisionRepeat-1` passed. Latest repeat band records summon block as `blocks 2/2; hits 0/0`, no-punish as `miss 1/1; counter 1/1; body 15/15`, boss-screen recovery as `answer cue 1/1; skill1 2/2`, and clean punish as `skill1 2/2; HP 0/0`; non-repeated exploratory rows stay labeled `not repeated`. Interpretation: this protects the ArkData/PGR premise from single-sample overreading before any runtime tuning or UI feedback work.
- 2026-06-28 batch evidence after core decision-signal repeat coverage: added `ForwardRiskEnergyProbe` and `NoSummonNoFire` to the repeatability policy set so the first two `Combat Decision Signal Matrix` rows are no longer single-sample reads. `dotnet build` passed and focused PlayMode `DecisionRepeatCoverage-1` passed. Latest repeat band records risk-for-EN as LV1 `3.8/3.8s`, HP `40/40`, ready cue `1/1`; ignored pressure as body `9/9`, HP `63.7/70.1`, and damage cues `15/15`, with both repeat rows `PASS`. Interpretation: the decision audit now repeatedly proves both the resource-risk incentive and the no-summon body-pressure cost before any UI/coaster, reward, or balance-tuning pass.
- 2026-06-28 batch evidence after support decision repeat coverage: added the delayed support recovery routes to the repeatability policy set and surfaced a `Repeat signal` column in the top `Support Decision Matrix`. `dotnet build` passed and focused PlayMode `SupportRepeat-1` passed. Latest repeat bands keep Slot1 recovery `CounterRecoveryClear PASS` with HP `40/40` and Skill1 `2/2`, Slot2 full-bank combo `CleanFollowupClear PASS` with HP `70.9/70.9`, Slot2 delayed recovery `CounterRecoveryClear PASS` with HP `50.8/50.8`, Slot3 immediate lockout `Running PASS`, and Slot3 delayed recovery `CleanFollowupClear PASS` with HP `70.9/70.9`, Skill1 `1/1`, suppress `2/2`. Interpretation: support summon route-choice and dominance reads are now backed by repeated batch evidence instead of single matrix samples; still no UI/coaster, reward, or balance-only change.
- 2026-06-28 report-language correction after repeat coverage: cleaned up `Runner Notes` so expected relock/recovery branches no longer look like generic failures. Physical Skill1 no-hit notes now distinguish boss-screen interception, boss-screen suppression, and counter-answer transfer from a true missing boss hit. Boss-screen blocked/ignored policies now record `counter final window pending until fresh summon answer` instead of `counter final window did not open before Skill1` when the policy intentionally withholds the fresh answer. `dotnet build` passed and focused PlayMode `RunnerNotes-1` passed. Interpretation: this is not a balance or UI change; it protects the ArkData/CombatPayload/PGR premise by keeping report evidence readable before the next structural attempt.
- 2026-06-28 batch evidence after support payoff verdict reporting: extended `Support Decision Matrix` with `Time/dmg` and `Payoff verdict` so support choices are judged by clear time, boss damage, HP exposure, recovery burden, and result together. First focused PlayMode `SupportPayoff-1` usefully failed because `ForwardRiskSlot3ThenDelayedRecoveryRoute` sometimes cleared without proving boss-screen suppression (`suppress 0/2`), exposing a runner setup gap instead of a balance issue. The runner now prepares an enemy boss screen before Skill1 only for LV3 delayed support payoff probes, so the vanguard branch consistently tests the intended boss-screen suppress state. `dotnet build` passed and focused PlayMode `SupportPayoff-2` passed. Latest matrix records Slot2 full-bank as `18.0s / 238.7` clean no-recovery payoff, Slot2 delayed as `19.6s / 309.6` high-damage recovery-burden payoff, and Slot3 delayed as `23.5s / 228.4` boss-screen suppress payoff with repeat `suppress 2/2`. Interpretation: do not blindly tune Slot2/Slot3 yet; the next decision should compare whether this tradeoff is fun enough or whether high-damage recovery is over-rewarding a mistimed marksman spend.
- 2026-06-28 structural correction after Slot2 delayed payoff audit: capped the over-rewarded Slot2 delayed recovery branch without changing the support roster premise. ArkData reason: a low-cost `SUPPRESS_ENEMY_FRONT` support should buy frontline tempo, not become an unattended passive boss-body DPS stage result. CombatPayload reason: the BacklineMarksman `SupportSummon Target -> Effect` should hit the enemy frontline and leave recovery burden intact; hostile body contact with the boss is now a separate damage policy instead of leaking through generic summon clash damage. PGR reason: a mistimed support spend should recover through the lock/unlock branch, while the high-cost vanguard branch owns the boss-screen suppression payoff. Narrow changes: canonical Slot2 support volley count is now `2` instead of `3`, `SummonFrontlineClash` has a hostile-body damage multiplier/cap, BacklineMarksman sets hostile boss-body damage to `0`, and Slot1/Slot3 keep default body damage behavior. `dotnet build` passed and focused PlayMode `Slot2BodyGate-2` passed. Latest matrix records Slot2 full-bank combo as `17.9s / 238.7`, `CleanFollowupClear`, boss damage `Player 208.0 + Ally 30.7`; Slot2 delayed recovery as `19.6s / 256.0`, `CounterRecoveryClear`, boss damage `Player 208.0 + Ally 48.0`, matching the Slot1 recovery baseline `256.0` with payoff verdict `recovery baseline, burden`. Slot2 identity remains intact in the standalone support route: volley waves `2`, blocks `0`, support projectile hits `4/4`, physical player hits `4/4`, and boss damage `0.0`. Interpretation: delayed marksman spend is no longer a high-damage buff; it is a recovery baseline with extra cost/burden, while full-bank Slot2 still earns a clean no-recovery combo and Slot3 still owns line-hold plus boss-screen suppression. Boundary: no UI/coaster feedback, reward economy, broad VFX/audio, new scene, or balance-only retune was introduced.
- 2026-06-28 batch evidence after summon readiness/cooldown audit: added `Summon Slot Readiness/Cooldown Matrix` to the policy report and JSON without changing runtime balance or UI prefabs. ArkData/Blue Archive reason: support choice should be inspected as a shared EN bank with slot-specific costs and per-slot readiness/cooldown, not mistaken for one global cooldown button. CombatPayload reason: a blocked summon input must report whether the failed gate is `Resource` or `Cooldown` before presentation/UI polish. `dotnet build` passed and focused PlayMode `SlotReadiness-1` passed. Latest matrix records Slot2 full-bank combo with Slot2 cooldown `1.5s -> 0.8s`, Slot1 cooldown `0.0s -> 1.3s`, mana `100 -> 117.3`, and Slot1 `used`, proving Slot2 cooldown does not lock Slot1 when shared EN can pay the cost. Slot3 immediate lockout records Slot3 cooldown `1.5s -> 1.5s`, Slot1 cooldown `0.0s -> 0.0s`, mana `0 -> 0`, and Slot1 `blocked:Requires 100 EN`, proving the lockout is shared EN cost, not global cooldown. Slot2/Slot3 delayed recovery both show support cooldown cleared to `0.0s` before Slot1 reopens, then Slot1 starts its own `1.3s` cooldown after use. Interpretation: the runtime model is shared EN plus separate slot costs/cooldowns; the remaining player-facing risk is UI/readout ambiguity, so any later coaster/HUD pass must preserve this measured Resource-vs-Cooldown distinction instead of inventing a new combat rule. Boundary: no new scene, no reward economy, no broad HUD replacement, and no balance-only retune was introduced.
- 2026-06-28 readout correction after readiness/cooldown audit: connected the review mobile HUD summon labels and radial fill to the already-measured shared EN + per-slot cost/cooldown model. ArkData/Blue Archive reason: the player-facing summon choice must not make Slot2/Slot3 look globally ready just because LV1 EN exists. CombatPayload reason: blocked input presentation now distinguishes `Resource` (`NEED 200 EN` / `NEED 300 EN`) from `Cooldown` (`CD 0.8s`) before any final coaster/icon work. Narrow change: `BossBarrageLaneReviewMobileHudLabels` now gates support READY by `MinimumSummonTier` and `RequiredSummonMana`, shows cooldown first, and exposes cost/cooldown-aware fill helpers used by `BossBarrageLaneReviewMobileHud`; runtime combat balance, profiles, scenes, reward hooks, and final UI prefabs were not changed. `dotnet build` passed, focused PlayMode `MobileHudSummonReadoutsSeparateSharedManaCostAndSlotCooldown` passed, and the focused policy report `HudReadout-1` passed. Unit evidence: at 100 EN Slot1 reads `READY LV1`, Slot2 `NEED 200 EN` with fill `0.5`, Slot3 `NEED 300 EN` with fill `0.33`; at 200 EN Slot2 reads `READY LV2` and Slot3 stays `NEED 300 EN`; while Slot2 cooldown is `0.8s`, Slot2 reads `CD 0.8s` and Slot3 remains resource-gated. Latest policy report still records all four structural axes `PASS`, support decision repeat bands `PASS`, readiness matrix Slot2 cooldown not locking Slot1, and Slot3 immediate blocked by shared EN rather than global cooldown. Boundary preserved: this is a readout alignment pass, not UI/coaster polish, reward economy, broad HUD replacement, or combat balance retune.
- 2026-06-28 batch evidence after HUD/coaster readout matrix: promoted the mobile HUD label/fill values into the policy report and JSON as `Summon HUD Readiness Readout Matrix`. ArkData/Blue Archive reason: a visible EX-like coaster must expose the same shared EN + per-slot cost/cooldown decision as the combat route data, not only pass a helper unit test. CombatPayload reason: the `Resource` or `Cooldown` gate now has inspectable presentation/readout evidence immediately before Slot1 is attempted. `dotnet build` passed and focused PlayMode report `HudReadoutMatrix-2` passed with JSON parse verification. Latest matrix records Slot2 full-bank combo as support HUD `S2 ARROW / CD 0.8s` fill `0.44` pulse `off` while Slot1 HUD is `S1 SHIELD / READY LV1` fill `1.00` pulse `ON`; Slot3 immediate lockout as `S3 TANK / CD 1.5s` fill `0.00` with Slot1 `LV1 0%` fill `0.00`, proving EN lockout rather than global cooldown; Slot2 delayed and Slot3 delayed as support `NEED 200 EN` / `NEED 300 EN` while Slot1 is `READY LV1` pulse `ON`. Boundary preserved: no new HUD prefab, icon assets, tutorial/proxy overlay, reward economy, combat balance retune, or scene change.
- 2026-06-28 batch evidence after review overlay readiness alignment: aligned `BossBarrageLaneReviewHud` with the same shared EN + per-slot cost/cooldown readout used by the mobile summon HUD, and added the overlay string to `Summon HUD Readiness Readout Matrix` / JSON. ArkData/Blue Archive reason: every visible review surface must show the EX-like slot choice as slot-specific cost/cooldown over a shared bank, not as a single global summon cooldown. CombatPayload reason: the presentation row now preserves the `Resource` vs `Cooldown` gate immediately before Slot1 input across both mobile and overlay readouts. `dotnet build` passed, focused PlayMode `ReviewHudSummonReadoutShowsSlotCostsAndCooldowns` passed, and focused policy report `OverlayHudReadout-2` passed. Latest matrix records Slot2 full-bank as overlay `S1 ready LV1 / S2 CD 0.8s / S3 need 300 EN`, Slot3 immediate lockout as `S1 LV1 0% / S2 need 200 EN / S3 CD 1.5s`, and both delayed recovery rows as `S1 ready LV1` while Slot2/Slot3 remain `need 200/300 EN`. Interpretation: the UI ambiguity that made the old structure appear global is now covered by batch evidence, but this is still readout alignment rather than final coaster/icon/audio polish. Boundary preserved: no runtime balance change, reward economy, broad HUD replacement, new scene, or tutorial overlay was introduced.
- 2026-06-28 batch evidence after result-overlay motivation reporting: connected `BossBarrageLaneReviewOverlayHud` result readouts to the policy report metrics so stage-result motivation is judged on the player-visible overlay surface, not only the internal completion record. ArkData/NIKKE reason: stage result rows should expose outcome, reward/state hook, and next-run motivation together before a real reward economy exists. CombatPayload reason: `Status/Result -> Presentation` now includes overlay title, summary, route, reward hook, and next objective. `dotnet build` passed and focused policy report `ResultOverlayMotivation-1` passed. Latest `Stage Result Motivation Matrix` records overlay motivation for no-summon/gun-only fails (`Failure analysis logged` + `protect HP`), clean summon confirm (`Clean survival logged` + `confirming before counter pressure`), counter recovery (`Counter recovery logged` + `answer counter pressure earlier`), Slot2 marksman clear (`Marksman combo logged` + `spend Slot2 from a full bank`), and Slot3 vanguard clear (`Vanguard payoff logged` + `commit Slot3 when line safety is worth the delayed main answer`). Boundary preserved: no payout persistence, item grant, campaign clear, stage select economy, broad UI prefab replacement, or combat balance retune was introduced.
- 2026-06-28 batch evidence after result token/next-state hook reporting: translated the NIKKE `reward_id / next state` stage-result gap into review-only result tokens on `BossBarragePocketReviewOwner.RouteResultRecord`, not into a payout economy. ArkData/NIKKE reason: result rows should carry a stable identifier that downstream UI/economy can later consume. CombatPayload reason: the committed `Result` status now contains both Presentation copy and structured IDs. `dotnet build` passed and focused policy report `ResultToken-1` passed. Latest `Stage Result Motivation Matrix` records `review.failure.hp_pressure -> next.practice.hp_protection` for no-summon/gun-only fails, `review.clear.clean_followup -> next.practice.clean_followup_confirm` for clean summon confirms, `review.clear.counter_recovery -> next.practice.counter_answer_timing` for counter recovery, `review.clear.marksman_combo -> next.practice.slot2_full_bank_combo` for Slot2 full-bank clears, and `review.clear.vanguard_payoff -> next.practice.slot3_vanguard_payoff` for Slot3 vanguard payoff. Boundary preserved: these IDs are review/practice hooks only; no reward persistence, item grant, campaign clear, stage-select economy, broad UI prefab replacement, or combat balance retune was introduced.
- 2026-06-28 report alignment after result token summary: updated the top `ArkData Coverage Summary` so the NIKKE stage-result row names the review-only result token/next-state hook evidence instead of hiding it deeper in the motivation matrix. `dotnet build` passed and focused policy report `ResultTokenSummary-1` passed. Latest summary now shows fail `review.failure.hp_pressure->next.practice.hp_protection`, clean `review.clear.clean_followup->next.practice.clean_followup_confirm`, and counter recovery `review.clear.counter_recovery->next.practice.counter_answer_timing` while still marking persistence/campaign reward as intentionally out of V1 scope.
- 2026-06-28 batch evidence after physical pressure conversion matrix: promoted the physical LinePressure branch into a top-level `Physical Pressure Conversion Matrix` and added repeatability assertions so forward danger, summon conversion, unconfirmed relock, and Skill1 payoff are judged together. ArkData reason: the same pressure slot should prove backline safety versus forward-risk hit cost before route payoff is discussed. CombatPayload reason: the row now reads Target/Hit -> Block/Status -> NoHit/Counter or Skill1/Result instead of scattering those numbers through raw tables. PGR reason: lock/unlock feel depends on proving the opened window either relocks when unconfirmed or completes when the player-authored punish lands. `dotnet build` passed and focused PlayMode `PhysicalConversion` passed. Latest matrix records backline `0/4` hits / `0.0` damage, forward-risk `4/4` hits / `40.0` damage with repeat floor `hits 3/4; dmg 30/40`, summon block `2` blocks with `0` player hits / `0.0` damage, no-punish `miss 1` / `counter 1` / body `15`, and Skill1 confirm `CleanFollowupClear` with player/summon boss payoff `208.0/78.7`. Boundary preserved: no runtime balance, new scene, broad presentation pass, reward economy, or UI/coaster change was introduced.
- 2026-06-28 batch evidence after LV3 suppress result hook: promoted the direct high-tier boss-screen suppress payoff from generic `clean_survival` into `high_tier_suppress_clear` with token `review.clear.lv3_suppress -> next.practice.lv3_suppress_timing`, then fixed the exposed repeatability race by adding a narrow follow-up Skill1 impact-confirm grace. ArkData/NIKKE reason: the high-risk LV3 wait needs its own result/state hook before reward economy work. CombatPayload reason: Skill1 input during the follow-up state must carry into the projectile hit, instead of letting projectile travel collapse `Status -> Hit` into a false miss. PGR reason: an unlocked punish state can tolerate projectile-settle timing while still relocking if the hit never arrives. `dotnet build` passed and focused PlayMode `Lv3SuppressResult` passed. Latest repeatability records `ForwardRiskTier3DecisionRoute` as `CleanFollowupClear` in 3/3 runs, HP lost `70.912`, boss damage `128`, `skillProjectileHits 1`, `followupHitCount 1`, `followupMissCount 0`, `counterWaves 0`, and result token `review.clear.lv3_suppress`. Boundary preserved: no new scene, reward economy, broad UI/coaster, broad presentation pass, or balance-only damage retune was introduced.
- 2026-06-28 batch evidence after LV3 suppress hit-confirm presentation: preserved the high-cost LV3 cut-in while preventing the earned `SummonFollowupHit` micro-cue from disappearing behind a stronger active cinematic. ArkData/NIKKE reason: the high-risk wait now carries both result hook and readable payoff presentation before reward economy work. CombatPayload reason: `Status -> Hit -> Presentation` is recorded even when an existing cut-in should not be interrupted; the director stamps the hit frame overlay immediately instead of starting full sequence playback. PGR reason: a locked/cut-in state can still expose the confirmed punish impact without breaking the state lock. `dotnet build` passed and focused PlayMode `Lv3SuppressCinematic4` passed. Latest repeatability records `ForwardRiskTier3DecisionRoute` as `CleanFollowupClear` in 3/3 runs with HP lost `70.912`, boss damage `128`, suppress screen/camera/VFX `1/1/1`, micro hit min/max `1/1`, sequence hit `0/0`, and the detailed row `Director window/hit/miss 1/1/0`, hit frame overlays `1`, tier `3/0`. Boundary preserved: no new scene, reward economy, broad UI/coaster, broad presentation restoration, or balance-only damage retune was introduced.
- 2026-06-28 batch evidence after support stage-slot timeline reporting: promoted support summon choices into a `Support Stage-Slot Timeline Matrix` so the Slot2/Slot3 routes read as ordered stage slots instead of isolated balance rows. ArkData/NIKKE reason: support spend, support effect, main-answer gate, relock/payoff, and result hook should be visible before final UI/coaster or reward economy work. CombatPayload reason: `SupportSummon Trigger -> Effect -> Resource/MainAnswer -> Counter/Payoff -> Result` is now asserted and exported to JSON. PGR reason: wrong-timing support choices should show lockout or relock, while high-cost vanguard timing should show the suppress payoff. `dotnet build` passed and focused PlayMode `SupportStageTimeline` passed. Latest matrix records Slot2 full-bank as `SummonSlot2 cost 200 -> mana 100`, `Slot1 used after immediate`, `clean hit 2`, `support_marksman_clear`; Slot2 delayed recovery as `Slot1 used after 3.1s`, `counter relock 1`, `counter_recovery`; Slot3 immediate as `Slot1 blocked: Requires 100 EN`, unresolved `ScreenCurtain`; and Slot3 delayed payoff as `Slot1 used after 3.8s`, `boss-screen suppress 2/3`, `support_vanguard_clear`. Boundary preserved: no runtime balance retune, new scene, reward economy, final UI/coaster polish, or broad presentation restoration was introduced.
- 2026-06-28 batch evidence after route motivation/dominance reporting: added `Route Motivation/Dominance Matrix` to compare clean clear routes before any further feel or balance tuning. ArkData/NIKKE reason: stage routes should not collapse into the single fastest clear or one generic reward row; each clear route needs a distinct cost/payoff/result hook. CombatPayload reason: the report now compares `Status/Hit -> Result` payoff with the cost/risk that opened the route. PGR reason: fast clean punish, counter recovery, high-tier suppress, and support-assisted payoff should remain different lock/unlock outcomes. `dotnet build` passed and focused PlayMode `RouteDominance` passed. Latest matrix records physical block-punish as the fastest reference `5.6s`, HP `0.0`, boss `286.7`, `clean_survival`; `IntendedRoute` is now in repeatability and passes 3/3 at `8.6s`, HP `0.0`, boss `208.0`, `clean_survival`; counter recovery passes at `9.1s`, `counter_recovery`; LV3 suppress costs HP `70.9` and keeps `high_tier_suppress_clear`; Slot2 full-bank costs HP `70.9`, boss `238.7`, and keeps `support_marksman_clear`; Slot3 delayed costs HP `70.9`, waits `3.8s`, boss `199.7`, and keeps `support_vanguard_clear`. Interpretation: the fastest physical punish is allowed as a clean-execution reference, but future tuning should preserve route identities and only adjust a route when its cost/payoff reason collapses. Boundary preserved: no runtime balance retune, new scene, reward economy, final UI/coaster polish, or broad presentation restoration was introduced.
- 2026-06-28 report alignment after support result tokens: updated `ArkData Coverage Summary` so the NIKKE stage-result row also names the support route result hooks and review-only next-state tokens. `dotnet build` passed and focused PlayMode `ArkSummarySupport` passed. Latest summary now includes support marksman `support_marksman_clear` token `review.clear.marksman_combo->next.practice.slot2_full_bank_combo` and support vanguard `support_vanguard_clear` token `review.clear.vanguard_payoff->next.practice.slot3_vanguard_payoff`, alongside fail, clean, LV3 suppress, close-chain, and counter recovery evidence. Boundary preserved: the row remains `PARTIAL` only because reward/item persistence and campaign clear are intentionally out of the V1 combat slice.
